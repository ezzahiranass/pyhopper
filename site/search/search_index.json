{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"pyhopper","text":"<p>Declarative parametric 3D modeling in Python.</p> <p>pyhopper brings the node-graph paradigm of Grasshopper3D into pure Python. You define a model by composing components \u2014 each one a small, testable function that reads from and writes to DataTrees \u2014 just like wiring nodes on a Grasshopper canvas.</p> <pre><code>from pyhopper import (\n    CircleCmp, DivideCurve, Series, UnitZ,\n    Move, Rotate, Polygon, Merge,\n)\n\nbase   = CircleCmp(radius=20)\npts    = DivideCurve(base, count=12)\nz      = UnitZ()\nlevels = Series(start=0, step=3.5, count=20)\nangles = Series(start=0, step=3,   count=20)\n\nfloors  = Move(pts, z, levels.graft())\ntwisted = Rotate(floors, axis=z, angle=angles.graft())\npolys   = Polygon(twisted)\ntower   = Merge(polys)\n</code></pre>"},{"location":"#key-ideas","title":"Key ideas","text":"Concept Description Atom Immutable geometric primitive (<code>Point3d</code>, <code>Line</code>, <code>Circle</code>, <code>Polyline</code>, \u2026) DataTree Hierarchical container of Atoms \u2014 the unit of exchange between components Branch / Path A branch is a list of items at an address <code>{0;2;1}</code> inside a DataTree Component A processing node \u2014 takes DataTrees in, returns a DataTree out Graft / Flatten Tree operations that reshape data before it flows into the next component"},{"location":"#install","title":"Install","text":"<pre><code>pip install pyhopper\n</code></pre> <p>Or from source:</p> <pre><code>git clone https://github.com/your-org/pyhopper\npip install -e .\n</code></pre>"},{"location":"#quick-links","title":"Quick links","text":"<ul> <li>Getting Started \u2014 your first model in 10 minutes</li> <li>Concepts \u2014 understand DataTrees and how components work</li> <li>Reference \u2014 full auto-generated API docs</li> <li>Examples \u2014 annotated real-world models</li> </ul>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#installation","title":"Installation","text":"<pre><code>pip install pyhopper\n</code></pre> <p>pyhopper has no mandatory dependencies beyond the standard library. Optional geometry backends (<code>shapely</code>, <code>build123d</code>) unlock more components.</p>"},{"location":"getting-started/#your-first-model","title":"Your first model","text":"<p>We'll build a minimal twisted column grid \u2014 the same idea as the bundled tower example, reduced to its essence.</p>"},{"location":"getting-started/#1-create-a-base-circle-and-divide-it","title":"1. Create a base circle and divide it","text":"<pre><code>from pyhopper import CircleCmp, DivideCurve\n\ncircle = CircleCmp(radius=10)\npoints = DivideCurve(circle, count=8)\n# points is a DataTree: 1 branch {0}, 8 Point3d items\n</code></pre> <p><code>CircleCmp</code> produces a <code>Circle</code> atom. <code>DivideCurve</code> samples it at <code>count</code> evenly-spaced parameter values and returns <code>Point3d</code> atoms.</p>"},{"location":"getting-started/#2-stack-points-vertically","title":"2. Stack points vertically","text":"<pre><code>from pyhopper import Series, UnitZ, Move\n\nz      = UnitZ()                               # DataTree: 1 Vector3d (0,0,1)\nlevels = Series(start=0, step=3.0, count=10)  # DataTree: 10 floats\nfloors = Move(points, z, levels.graft())\n# floors: 10 branches {0;0}\u2026{0;9}, 8 Point3d each = 80 points\n</code></pre> <p><code>levels.graft()</code> turns the flat list of 10 heights into 10 branches of 1 item each. When <code>Move</code> sees 10 branches on one input and 1 branch on the other, it repeats the single branch for every step \u2014 producing one translated copy of the 8 base points for each floor level.</p>"},{"location":"getting-started/#3-close-each-floor-into-a-polygon","title":"3. Close each floor into a polygon","text":"<pre><code>from pyhopper import Polygon\n\npolys = Polygon(floors)\n# polys: 10 branches, 1 Polyline each\n</code></pre> <p><code>Polygon</code> uses <code>LIST</code> access \u2014 it receives the full list of points per branch and returns one closed <code>Polyline</code> per floor.</p>"},{"location":"getting-started/#4-export-to-glb","title":"4. Export to GLB","text":"<pre><code>from pyhopper.Utils.Exporters import export_glb\n\nexport_glb(polys, \"column_grid.glb\")\n</code></pre> <p>Open the file in any glTF viewer (Babylon.js sandbox, Blender, three.js editor).</p>"},{"location":"getting-started/#understanding-the-flow","title":"Understanding the flow","text":"<pre><code>CircleCmp(r=10)           \u2192 DataTree {0}: [Circle]\n    \u2193 DivideCurve(n=8)\n                           \u2192 DataTree {0}: [P0, P1, \u2026 P7]  (8 pts)\n    \u2193 Move(z, levels.graft())\n    levels = [0,3,6,\u202627]  \u2192 DataTree {0;0}\u2026{0;9}: [P0\u2026P7] per floor\n    \u2193 Polygon\n                           \u2192 DataTree {0;0}\u2026{0;9}: [Polyline] per floor\n</code></pre> <p>The tree structure is the memory of where data came from. When you graft, each item splits into its own branch so the next component can address them individually.</p>"},{"location":"getting-started/#next-steps","title":"Next steps","text":"<ul> <li>Read Concepts \u2192 Data Tree to understand branching deeply</li> <li>See the full Parametric Tower example with rotation and export</li> <li>Browse the Reference for every component's inputs and outputs</li> </ul>"},{"location":"concepts/","title":"Concepts","text":"<p>pyhopper is built on three interlocking ideas taken directly from Grasshopper's data model. Understanding them makes everything else predictable.</p> Concept One line Atoms Immutable geometric primitives \u2014 the leaves of every DataTree DataTree The hierarchical container that carries data between components Components Processing nodes \u2014 the only things allowed to transform data <p>Read them in order if you're new; jump to whichever is relevant if you're debugging a tree mismatch.</p>"},{"location":"concepts/atoms/","title":"Atoms","text":"<p>Atoms are the indivisible data units that flow through DataTrees. They are frozen dataclasses \u2014 immutable, hashable, and directly serializable to JSON.</p> <p>No geometry operations live on Atoms. All transformations belong in Components.</p>"},{"location":"concepts/atoms/#built-in-atoms","title":"Built-in atoms","text":"Atom Description <code>Point3d</code> A point in 3D space <code>(x, y, z)</code> <code>Vector3d</code> A direction vector <code>(x, y, z)</code> <code>Interval</code> A numeric domain <code>[start, end]</code> <code>Plane</code> An oriented coordinate frame (origin + normal + x-axis) <code>Line</code> A line segment between two <code>Point3d</code> values <code>Circle</code> A circle defined by a <code>Plane</code> and a radius <code>Arc</code> A circular arc defined by a <code>Plane</code>, radius, and angle interval <code>Polyline</code> An ordered sequence of <code>Point3d</code> values (optionally closed) <code>NurbsCurve</code> A NURBS curve (control points, weights, knots, degree) <code>Mesh</code> A polygonal mesh (vertices + face index lists) <code>Surface</code> A NURBS surface (control point grid, degree U/V) <code>Cylinder</code> A cylinder defined by a <code>Plane</code>, radius, and height"},{"location":"concepts/atoms/#using-atoms-directly","title":"Using atoms directly","text":"<p>Atoms are plain Python objects:</p> <pre><code>from pyhopper import Point3d, Vector3d, Plane\n\npt  = Point3d(1.0, 2.0, 3.0)\nvec = Vector3d(0, 0, 1).unitize()\npln = Plane.world_xy()\n\npt.x          # 1.0\nvec.length    # 1.0\npln.y_axis    # Vector3d(0, 1, 0)\n</code></pre>"},{"location":"concepts/atoms/#serialization","title":"Serialization","text":"<p>Every Atom serializes to a plain dict with a <code>\"type\"</code> key:</p> <pre><code>pt = Point3d(1, 2, 3)\npt.to_json()\n# {\"type\": \"Point3d\", \"x\": 1.0, \"y\": 2.0, \"z\": 3.0}\n\nPoint3d.from_json({\"type\": \"Point3d\", \"x\": 1.0, \"y\": 2.0, \"z\": 3.0})\n# Point3d(x=1.0, y=2.0, z=3.0)\n</code></pre> <p>The global <code>ATOM_REGISTRY</code> maps type strings to classes, so DataTree deserialization dispatches automatically \u2014 you never need to specify the class manually.</p>"},{"location":"concepts/atoms/#defining-a-custom-atom","title":"Defining a custom Atom","text":"<p>Subclass <code>Atom</code>, decorate with <code>@dataclass(frozen=True)</code>, set <code>atom_type</code>, and implement <code>to_json</code> / <code>from_json</code>. It auto-registers.</p> <pre><code>from dataclasses import dataclass\nfrom typing import ClassVar\nfrom pyhopper import Atom\n\n@dataclass(frozen=True)\nclass ColoredPoint(Atom):\n    atom_type: ClassVar[str] = \"ColoredPoint\"\n\n    x: float = 0.0\n    y: float = 0.0\n    z: float = 0.0\n    r: int = 255\n    g: int = 255\n    b: int = 255\n\n    def to_json(self) -&gt; dict:\n        return {\"type\": \"ColoredPoint\",\n                \"x\": self.x, \"y\": self.y, \"z\": self.z,\n                \"r\": self.r, \"g\": self.g, \"b\": self.b}\n\n    @classmethod\n    def from_json(cls, data: dict) -&gt; \"ColoredPoint\":\n        return cls(data[\"x\"], data[\"y\"], data[\"z\"],\n                   data[\"r\"], data[\"g\"], data[\"b\"])\n</code></pre>"},{"location":"concepts/atoms/#api-reference","title":"API reference","text":"<p>See the full Atoms reference.</p>"},{"location":"concepts/components/","title":"Components","text":"<p>A Component is a processing node that reads DataTrees, calls <code>generate()</code> once per matched item (or branch, or tree), and returns a new DataTree.</p> <p>Calling a component looks like a function call. The result is always a <code>ComponentResult</code> that also exposes named secondary outputs as attributes.</p>"},{"location":"concepts/components/#anatomy-of-a-component","title":"Anatomy of a component","text":"<pre><code>from pyhopper.Core.Component import Component, InputParam, OutputParam, Access\n\nclass Move(Component):\n    inputs = [\n        InputParam(\"geometry\", None, Access.ITEM),\n        InputParam(\"direction\", Vector3d, Access.ITEM, default=None),\n        InputParam(\"distance\", float, Access.ITEM, default=1.0),\n    ]\n    outputs = [OutputParam(\"geometry\")]\n\n    def generate(self, geometry=None, direction=None, distance=1.0):\n        return ...\n</code></pre> <ul> <li><code>inputs</code> declares the parameter names, types, access mode, and defaults.</li> <li><code>generate()</code> receives one matched item per input on each call.</li> <li>Whatever <code>generate()</code> returns is appended to the output branch for the current path.</li> </ul>"},{"location":"concepts/components/#access-modes","title":"Access modes","text":"Mode <code>generate()</code> receives Use when <code>Access.ITEM</code> One item per input per-item math / geometry ops <code>Access.LIST</code> One <code>list</code> per input (full branch) need to look at all items together <code>Access.TREE</code> Full <code>DataTree</code> per input need full tree awareness <p>The access mode is determined by the highest mode declared on any input.</p>"},{"location":"concepts/components/#the-solve-pipeline","title":"The solve pipeline","text":"<p>When you call <code>Move(geometry, direction, distance)</code>:</p> <ol> <li>Coerce inputs: every argument is wrapped in a DataTree via <code>DataTree.coerce()</code>.</li> <li>Match: <code>DataTree.match()</code> aligns branches and items across all input trees.</li> <li>Iterate: for each matched <code>(path, items)</code> tuple, call <code>generate(**kw)</code>.</li> <li>Collect: return values are appended to output branches, preserving path structure.</li> <li>Return: a <code>ComponentResult</code> is returned. Multi-output components expose secondary outputs as attributes.</li> </ol>"},{"location":"concepts/components/#graft-and-the-matching-engine","title":"Graft and the matching engine","text":"<p>The most important pattern in pyhopper is using <code>.graft()</code> to force a one-to-one relationship between parameters and geometry:</p> <pre><code>levels = Series(start=0, step=3.5, count=20)\n# {0}: [0, 3.5, 7, ..., 66.5]\n\nfloors = Move(points, z, levels.graft())\n# levels.graft() -&gt; {0;0}: [0]   {0;1}: [3.5]  ...  {0;19}: [66.5]\n#\n# Match:  points {0}:12 items   x   levels {0;0}...{0;19}: 1 item each\n# Result: 20 branches x 12 points = 240 output items\n</code></pre> <p>Without <code>.graft()</code>, all 20 heights would be in one branch and zip against the 12 points via longest-list, giving only 20 outputs total, all at the same path <code>{0}</code>.</p>"},{"location":"concepts/components/#multi-output-components","title":"Multi-output components","text":"<p>If <code>generate()</code> returns a tuple, each element maps to the corresponding output:</p> <pre><code>class DeconstructPoint(Component):\n    inputs = [InputParam(\"point\", Point3d, Access.ITEM)]\n    outputs = [OutputParam(\"x\"), OutputParam(\"y\"), OutputParam(\"z\")]\n\n    def generate(self, point=None):\n        return point.x, point.y, point.z\n\nresult = DeconstructPoint(some_points)\nx_values = result.x\ny_values = result.y\nz_values = result.z\n</code></pre>"},{"location":"concepts/components/#defining-a-new-component","title":"Defining a new component","text":"<ol> <li>Subclass <code>Component</code>.</li> <li>Declare <code>inputs</code> and <code>outputs</code>.</li> <li>Implement <code>generate()</code>; it must be stateless and side-effect-free.</li> <li>Register it in <code>__init__.py</code> if you want it importable from the top level.</li> </ol> <pre><code>class Scale(Component):\n    inputs = [\n        InputParam(\"geometry\", None, Access.ITEM),\n        InputParam(\"factor\", float, Access.ITEM, default=1.0),\n    ]\n    outputs = [OutputParam(\"geometry\")]\n\n    def generate(self, geometry=None, factor=1.0):\n        return ...\n</code></pre> <p>That is all. The entire DataTree plumbing is inherited from <code>Component</code>. The component's math or geometry logic should live directly inside <code>generate()</code>.</p>"},{"location":"concepts/components/#api-reference","title":"API reference","text":"<p>See the full Component reference, including <code>Component</code>, <code>InputParam</code>, <code>OutputParam</code>, <code>Access</code>, and <code>ComponentResult</code>.</p>"},{"location":"concepts/data-tree/","title":"Data Tree","text":"<p>A <code>DataTree</code> is an ordered dictionary of Branches, each addressed by a Path.</p> <pre><code>DataTree\n \u251c\u2500\u2500 {0;0} \u2192 [Point3d(0,0,0), Point3d(5,0,0), Point3d(10,0,0)]\n \u251c\u2500\u2500 {0;1} \u2192 [Point3d(0,0,3), Point3d(5,0,3), Point3d(10,0,3)]\n \u2514\u2500\u2500 {0;2} \u2192 [Point3d(0,0,6), Point3d(5,0,6), Point3d(10,0,6)]\n</code></pre>"},{"location":"concepts/data-tree/#path","title":"Path","text":"<p>A <code>Path</code> is an immutable tuple of non-negative integers printed in Grasshopper notation:</p> <pre><code>from pyhopper import Path\n\np = Path(0, 2, 1)   # address {0;2;1}\np.depth             # 3\nstr(p)              # '{0;2;1}'\n</code></pre> <p>Paths are compared and sorted lexicographically, so <code>{0;1}</code> comes before <code>{0;2}</code>.</p>"},{"location":"concepts/data-tree/#branch","title":"Branch","text":"<p>A <code>Branch</code> is a plain Python list that knows its own path:</p> <pre><code>from pyhopper import Branch, Path\n\nb = Branch(Path(0, 1), [10, 20, 30])\nb.path   # Path(0, 1)\nb[0]     # 10\n</code></pre>"},{"location":"concepts/data-tree/#creating-trees","title":"Creating trees","text":"<pre><code>from pyhopper import DataTree, Path\n\n# Single item at {0}\ntree = DataTree.from_item(42)\n\n# Flat list at {0}\ntree = DataTree.from_list([1, 2, 3])\n\n# Explicit branch mapping\ntree = DataTree.from_branches({\n    Path(0): [1, 2, 3],\n    Path(1): [4, 5, 6],\n})\n\n# Coerce any value (DataTree passes through, scalars wrap to {0})\ntree = DataTree.coerce(some_value)\n</code></pre>"},{"location":"concepts/data-tree/#tree-operations","title":"Tree operations","text":"<p>All operations return a new DataTree \u2014 trees are never mutated.</p>"},{"location":"concepts/data-tree/#graft","title":"<code>graft()</code>","text":"<p>Each item becomes its own single-item branch. Item at <code>{a;b}[i]</code> moves to <code>{a;b;i}[0]</code>.</p> <pre><code>tree = DataTree.from_list([10, 20, 30])\n# {0}: [10, 20, 30]\n\ngrafted = tree.graft()\n# {0;0}: [10]\n# {0;1}: [20]\n# {0;2}: [30]\n</code></pre> <p>When to use it: You have a list of parameters (heights, angles, counts) and want each one to pair with a separate copy of the geometry in the next component.</p>"},{"location":"concepts/data-tree/#flatten","title":"<code>flatten()</code>","text":"<p>Collapse all branches into a single branch at <code>{0}</code>.</p> <pre><code>tree.flatten()\n# {0}: [10, 20, 30, 40, ...]\n</code></pre>"},{"location":"concepts/data-tree/#simplify","title":"<code>simplify()</code>","text":"<p>Remove the longest common path prefix from all branches.</p> <pre><code># {0;0}: [a]\n# {0;1}: [b]\n# simplify \u2192 {0}: [a]  {1}: [b]\n</code></pre>"},{"location":"concepts/data-tree/#flip_matrix","title":"<code>flip_matrix()</code>","text":"<p>Transpose branches and items: N branches of M items \u2192 M branches of N items.</p>"},{"location":"concepts/data-tree/#data-matching","title":"Data matching","text":"<p>When a component has multiple inputs, their trees are matched before <code>generate()</code> is called. The default rule is Longest List (same as Grasshopper's default):</p> <ul> <li>Branch matching: the tree with the most branches drives iteration; shorter trees   repeat their last branch.</li> <li>Item matching within a branch: shorter lists repeat their last item.</li> </ul> <pre><code># geometry:  {0}: [A, B, C]          (1 branch, 3 items)\n# distance:  {0;0}: [1]              (grafted, 1 item each)\n#            {0;1}: [2]\n#            {0;2}: [3]\n#\n# Result: 3 calls per level \u2192 9 output items across 3 branches\n</code></pre> <p>Other rules (<code>SHORTEST_LIST</code>, <code>CROSS_REFERENCE</code>) can be set on the component:</p> <pre><code>class MyComp(Component):\n    match_rule = MatchRule.CROSS_REFERENCE\n</code></pre>"},{"location":"concepts/data-tree/#serialization","title":"Serialization","text":"<p>Every DataTree and every Atom implements <code>to_json()</code> / <code>from_json()</code>:</p> <pre><code>json_str = tree.to_json_string(indent=2)\nrestored = DataTree.from_json(json.loads(json_str))\n</code></pre> <p>Items are dispatched by their <code>\"type\"</code> field through <code>ATOM_REGISTRY</code>, so any custom Atom you register is automatically round-trippable.</p>"},{"location":"concepts/data-tree/#api-reference","title":"API reference","text":"<p>See the full DataTree reference, Branch reference, and Path reference.</p>"},{"location":"examples/tower/","title":"Parametric Tower","text":"<p>A twisting tower with a columnar perimeter, a solid core, and a GLB export. This is the canonical pyhopper example \u2014 every concept from the framework appears here.</p> Examples/first_test.py<pre><code>from pyhopper import (\n    CircleCmp, DivideCurve, Series, UnitZ,\n    Move, Rotate, Polygon, CylinderCmp, LineCmp,\n    Merge,\n)\nfrom pyhopper.Utils.Exporters import export_glb\n\nNUM_FLOORS   = 20\nNUM_COLUMNS  = 12\nFLOOR_HEIGHT = 3.5   # m\nBASE_RADIUS  = 20.0  # m\nTWIST_DEG    = 3.0   # degrees per floor\nCORE_RADIUS  = 6.0   # m\n\n# \u2500\u2500 1. Base geometry \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nbase_circle  = CircleCmp(radius=BASE_RADIUS)\ngrid_points  = DivideCurve(base_circle, count=NUM_COLUMNS)\n# grid_points: {0} \u2192 [P0 \u2026 P11]  (12 points on a circle of r=20)\n\n# \u2500\u2500 2. Unit vectors \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nz_vec = UnitZ()\n# z_vec: {0} \u2192 [Vector3d(0,0,1)]  \u2014 reused as a DataTree throughout\n\n# \u2500\u2500 3. Lift points to each floor level \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nlevels       = Series(start=0, step=FLOOR_HEIGHT, count=NUM_FLOORS)\nfloor_points = Move(grid_points, z_vec, levels.graft())\n# levels.graft(): {0;0}\u2192[0]  {0;1}\u2192[3.5]  \u2026  {0;19}\u2192[66.5]\n# floor_points:   20 branches \u00d7 12 pts = 240 Point3d\n\n# \u2500\u2500 4. Rotate each floor \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nangles         = Series(start=0, step=TWIST_DEG, count=NUM_FLOORS)\nrotated_floors = Rotate(floor_points, axis=z_vec, angle=angles.graft())\n# rotated_floors: same tree shape, points rotated per branch\n\n# \u2500\u2500 5. Close each floor into a polygon \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nfloor_polygons = Polygon(rotated_floors)\n# floor_polygons: 20 branches, 1 Polyline each\n\n# \u2500\u2500 6. Vertical columns \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ntop_points = Move(grid_points, z_vec, FLOOR_HEIGHT * NUM_FLOORS)\ncolumns    = LineCmp(grid_points, top_points)\n# columns: {0} \u2192 12 Line atoms\n\n# \u2500\u2500 7. Central core \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ncore = CylinderCmp(radius=CORE_RADIUS, height=FLOOR_HEIGHT * NUM_FLOORS)\n\n# \u2500\u2500 8. Merge and export \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ntower = Merge(floor_polygons, columns, core)\nexport_glb(tower, \"tower.glb\")\n</code></pre>"},{"location":"examples/tower/#data-flow-diagram","title":"Data flow diagram","text":"<pre><code>CircleCmp(r=20)                 \u2192 {0}: [Circle]\n    \u2502\n    \u2514\u2500 DivideCurve(n=12)        \u2192 {0}: [P0\u2026P11]  12 pts\n           \u2502\n           \u251c\u2500 Move(z, levels\u2191)  \u2190 levels = Series(0, 3.5, 20).graft()\n           \u2502                    \u2192 {0;0}\u2026{0;19}: [P0\u2026P11] each  (240 pts)\n           \u2502       \u2502\n           \u2502       \u2514\u2500 Rotate(z, angles\u2191)  \u2190 angles = Series(0, 3\u00b0, 20).graft()\n           \u2502                              \u2192 240 rotated pts, same structure\n           \u2502               \u2502\n           \u2502               \u2514\u2500 Polygon     \u2192 {0;0}\u2026{0;19}: [Polyline]  (20 floors)\n           \u2502\n           \u2514\u2500 Move(z, 70)       \u2192 {0}: [P0'\u2026P11']  top pts\n                   \u2502\n                   \u2514\u2500 LineCmp   \u2192 {0}: [L0\u2026L11]  12 columns\n\nCylinderCmp(r=6, h=70)          \u2192 {0}: [Cylinder]\n\nMerge(floors, columns, core)    \u2192 {0;0}\u2026{0;19}: [Polyline]\n                                   {0}: [L0\u2026L11, Cylinder]\n    \u2502\n    \u2514\u2500 export_glb \u2192 tower.glb\n</code></pre>"},{"location":"examples/tower/#what-to-try-next","title":"What to try next","text":"<ul> <li>Change <code>TWIST_DEG</code> to <code>0</code> \u2014 all floors align, no twist</li> <li>Change <code>NUM_COLUMNS</code> to <code>6</code> \u2014 hexagonal floor plate</li> <li>Add a <code>Loft</code> component between floors to create a surface skin</li> <li>Increase <code>NUM_FLOORS</code> and <code>BASE_RADIUS</code> \u2014 everything scales automatically</li> </ul>"},{"location":"reference/SUMMARY/","title":"SUMMARY","text":"<ul> <li>Reference<ul> <li>Core<ul> <li>Atoms</li> <li>DataTree</li> <li>Branch</li> <li>Path</li> <li>Component</li> </ul> </li> <li>Components<ul> <li>Curve<ul> <li>Division<ul> <li>DivideCurve</li> </ul> </li> <li>Primitive<ul> <li>Circle</li> <li>Circle3Pt</li> <li>CircleCNR</li> <li>Cylinder</li> <li>Line</li> <li>LineSDL</li> <li>Polygon</li> <li>Rectangle</li> </ul> </li> </ul> </li> <li>Maths<ul> <li>Operators<ul> <li>Absolute</li> <li>Addition</li> <li>Division</li> <li>Factorial</li> <li>IntegerDivision</li> <li>Modulus</li> <li>Multiplication</li> <li>Negative</li> <li>Power</li> <li>Subtraction</li> </ul> </li> <li>Series</li> </ul> </li> <li>Params<ul> <li>Input<ul> <li>NumberSlider</li> </ul> </li> </ul> </li> <li>Sets<ul> <li>Tree<ul> <li>Merge</li> </ul> </li> </ul> </li> <li>Vector<ul> <li>Plane<ul> <li>ConstructPlane</li> </ul> </li> <li>Vector<ul> <li>Amplitude</li> <li>Angle</li> <li>CrossProduct</li> <li>DeconstructVector</li> <li>DotProduct</li> <li>Reverse</li> <li>Rotate</li> <li>UnitX</li> <li>UnitY</li> <li>UnitZ</li> <li>Vector2Pt</li> <li>VectorLength</li> <li>VectorXYZ</li> </ul> </li> </ul> </li> </ul> </li> <li>Utils<ul> <li>Exporters<ul> <li>glb</li> </ul> </li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/Components/Curve/Division/DivideCurve/","title":"DivideCurve","text":""},{"location":"reference/Components/Curve/Division/DivideCurve/#pyhopper.Components.Curve.Division.DivideCurve","title":"<code>pyhopper.Components.Curve.Division.DivideCurve</code>","text":"<p>DivideCurve - Divide a curve into evenly spaced points.</p>"},{"location":"reference/Components/Curve/Division/DivideCurve/#pyhopper.Components.Curve.Division.DivideCurve.DivideCurve","title":"<code>DivideCurve</code>","text":"<p>               Bases: <code>Component</code></p> <p>Divide a curve into evenly spaced <code>AtomicPoint</code> samples.</p> <p>Accepts an <code>AtomicCircle</code>, <code>AtomicArc</code>, <code>AtomicPolyline</code>, or <code>AtomicNurbsCurve</code> and returns <code>count</code> points in a single output branch per input curve.</p> Source code in <code>pyhopper\\Components\\Curve\\Division\\DivideCurve.py</code> <pre><code>class DivideCurve(Component):\n    \"\"\"Divide a curve into evenly spaced ``AtomicPoint`` samples.\n\n    Accepts an ``AtomicCircle``, ``AtomicArc``, ``AtomicPolyline``, or\n    ``AtomicNurbsCurve`` and returns\n    ``count`` points in a single output branch per input curve.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"curve\", None, Access.ITEM),\n        InputParam(\"count\", int, Access.ITEM, default=10),\n    ]\n    outputs = [OutputParam(\"points\")]\n\n    def generate(self, curve=None, count=10):\n        segment_count = int(count)\n\n        if isinstance(curve, AtomicCircle):\n            center = curve.plane.origin\n            radius = curve.radius\n            x_axis = curve.plane.x_axis\n            y_axis = curve.plane.y_axis\n\n            return [\n                AtomicPoint(\n                    center.x + radius * (math.cos((2.0 * math.pi * index) / segment_count) * x_axis.x + math.sin((2.0 * math.pi * index) / segment_count) * y_axis.x),\n                    center.y + radius * (math.cos((2.0 * math.pi * index) / segment_count) * x_axis.y + math.sin((2.0 * math.pi * index) / segment_count) * y_axis.y),\n                    center.z + radius * (math.cos((2.0 * math.pi * index) / segment_count) * x_axis.z + math.sin((2.0 * math.pi * index) / segment_count) * y_axis.z),\n                )\n                for index in range(segment_count)\n            ]\n\n        if isinstance(curve, AtomicLine):\n            return [\n                AtomicPoint(\n                    curve.start.x + (index / segment_count) * (curve.end.x - curve.start.x),\n                    curve.start.y + (index / segment_count) * (curve.end.y - curve.start.y),\n                    curve.start.z + (index / segment_count) * (curve.end.z - curve.start.z),\n                )\n                for index in range(segment_count + 1)\n            ]\n\n        raise TypeError(f\"Cannot divide curve of type {type(curve).__name__}\")\n</code></pre>"},{"location":"reference/Components/Curve/Primitive/Circle/","title":"Circle","text":""},{"location":"reference/Components/Curve/Primitive/Circle/#pyhopper.Components.Curve.Primitive.Circle","title":"<code>pyhopper.Components.Curve.Primitive.Circle</code>","text":"<p>Circle - Create a circle from a radius (and optional plane).</p>"},{"location":"reference/Components/Curve/Primitive/Circle/#pyhopper.Components.Curve.Primitive.Circle.Circle","title":"<code>Circle</code>","text":"<p>               Bases: <code>Component</code></p> <p>Create an <code>AtomicCircle</code> atom from a radius and an optional plane.</p> Source code in <code>pyhopper\\Components\\Curve\\Primitive\\Circle.py</code> <pre><code>class Circle(Component):\n    \"\"\"Create an ``AtomicCircle`` atom from a radius and an optional plane.\"\"\"\n\n    inputs = [\n        InputParam(\"radius\", float, Access.ITEM, default=1.0),\n        InputParam(\"plane\", AtomicPlane, Access.ITEM, default=None, optional=True),\n    ]\n    outputs = [OutputParam(\"circle\")]\n\n    def generate(self, radius=1.0, plane=None):\n        if plane is None:\n            plane = AtomicPlane.world_xy()\n        return AtomicCircle(plane=plane, radius=float(radius))\n</code></pre>"},{"location":"reference/Components/Curve/Primitive/Circle3Pt/","title":"Circle3Pt","text":""},{"location":"reference/Components/Curve/Primitive/Circle3Pt/#pyhopper.Components.Curve.Primitive.Circle3Pt","title":"<code>pyhopper.Components.Curve.Primitive.Circle3Pt</code>","text":"<p>Circle3Pt - Create a circle through three points.</p>"},{"location":"reference/Components/Curve/Primitive/Circle3Pt/#pyhopper.Components.Curve.Primitive.Circle3Pt.Circle3Pt","title":"<code>Circle3Pt</code>","text":"<p>               Bases: <code>Component</code></p> <p>Create an <code>AtomicCircle</code> through three non-collinear points.</p> <p>Returns the circle together with the derived plane and radius.</p> Source code in <code>pyhopper\\Components\\Curve\\Primitive\\Circle3Pt.py</code> <pre><code>class Circle3Pt(Component):\n    \"\"\"Create an ``AtomicCircle`` through three non-collinear points.\n\n    Returns the circle together with the derived plane and radius.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"a\", AtomicPoint, Access.ITEM),\n        InputParam(\"b\", AtomicPoint, Access.ITEM),\n        InputParam(\"c\", AtomicPoint, Access.ITEM),\n    ]\n    outputs = [\n        OutputParam(\"circle\"),\n        OutputParam(\"plane\"),\n        OutputParam(\"radius\", float),\n    ]\n\n    def generate(self, a=None, b=None, c=None):\n        ab = AtomicVector(b.x - a.x, b.y - a.y, b.z - a.z)\n        ac = AtomicVector(c.x - a.x, c.y - a.y, c.z - a.z)\n        normal = _cross_product(ab, ac)\n        normal_unit = normal.unitize()\n\n        if normal.length == 0:\n            raise ValueError(\"Circle3Pt requires three non-collinear points.\")\n\n        ex = ab.unitize()\n        ey = _cross_product(normal_unit, ex).unitize()\n\n        bx = ab.length\n        cx = _dot_product(ac, ex)\n        cy = _dot_product(ac, ey)\n        determinant = 2.0 * bx * cy\n\n        if determinant == 0:\n            raise ValueError(\"Circle3Pt requires three non-collinear points.\")\n\n        ux = bx / 2.0\n        uy = ((cx * cx) + (cy * cy) - (bx * cx)) / (2.0 * cy)\n\n        center = AtomicPoint(\n            a.x + ex.x * ux + ey.x * uy,\n            a.y + ex.y * ux + ey.y * uy,\n            a.z + ex.z * ux + ey.z * uy,\n        )\n        radius = math.sqrt(\n            (center.x - a.x) ** 2 +\n            (center.y - a.y) ** 2 +\n            (center.z - a.z) ** 2\n        )\n        plane = AtomicPlane(origin=center, normal=normal_unit, x_axis=ex)\n        circle = AtomicCircle(plane=plane, radius=radius)\n        return circle, plane, radius\n</code></pre>"},{"location":"reference/Components/Curve/Primitive/CircleCNR/","title":"CircleCNR","text":""},{"location":"reference/Components/Curve/Primitive/CircleCNR/#pyhopper.Components.Curve.Primitive.CircleCNR","title":"<code>pyhopper.Components.Curve.Primitive.CircleCNR</code>","text":"<p>CircleCNR - Create a circle from center, normal, and radius.</p>"},{"location":"reference/Components/Curve/Primitive/CircleCNR/#pyhopper.Components.Curve.Primitive.CircleCNR.CircleCNR","title":"<code>CircleCNR</code>","text":"<p>               Bases: <code>Component</code></p> <p>Create an <code>AtomicCircle</code> from a center, normal, and radius.</p> <p>Accepts a center point, a normal vector, and a radius, then constructs the corresponding circle in a derived plane.</p> Source code in <code>pyhopper\\Components\\Curve\\Primitive\\CircleCNR.py</code> <pre><code>class CircleCNR(Component):\n    \"\"\"Create an ``AtomicCircle`` from a center, normal, and radius.\n\n    Accepts a center point, a normal vector, and a radius, then constructs\n    the corresponding circle in a derived plane.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"center\", AtomicPoint, Access.ITEM),\n        InputParam(\"normal\", AtomicVector, Access.ITEM, default=None, optional=True),\n        InputParam(\"radius\", float, Access.ITEM, default=1.0),\n    ]\n    outputs = [OutputParam(\"circle\")]\n\n    def generate(self, center=None, normal=None, radius=1.0):\n        if normal is None:\n            normal = AtomicVector.unit_z()\n        normal_unit = normal.unitize()\n        x_axis = _orthonormal_x_axis(normal_unit)\n        plane = AtomicPlane(origin=center, normal=normal_unit, x_axis=x_axis)\n        return AtomicCircle(plane=plane, radius=float(radius))\n</code></pre>"},{"location":"reference/Components/Curve/Primitive/Cylinder/","title":"Cylinder","text":""},{"location":"reference/Components/Curve/Primitive/Cylinder/#pyhopper.Components.Curve.Primitive.Cylinder","title":"<code>pyhopper.Components.Curve.Primitive.Cylinder</code>","text":"<p>Cylinder - Create a cylinder from radius and height.</p>"},{"location":"reference/Components/Curve/Primitive/Cylinder/#pyhopper.Components.Curve.Primitive.Cylinder.Cylinder","title":"<code>Cylinder</code>","text":"<p>               Bases: <code>Component</code></p> <p>Create an <code>AtomicCylinder</code> atom from a radius and height.</p> Source code in <code>pyhopper\\Components\\Curve\\Primitive\\Cylinder.py</code> <pre><code>class Cylinder(Component):\n    \"\"\"Create an ``AtomicCylinder`` atom from a radius and height.\"\"\"\n\n    inputs = [\n        InputParam(\"radius\", float, Access.ITEM, default=1.0),\n        InputParam(\"height\", float, Access.ITEM, default=1.0),\n    ]\n    outputs = [OutputParam(\"cylinder\")]\n\n    def generate(self, radius=1.0, height=1.0):\n        return AtomicCylinder(\n            plane=AtomicPlane.world_xy(),\n            radius=float(radius),\n            height=float(height),\n        )\n</code></pre>"},{"location":"reference/Components/Curve/Primitive/Line/","title":"Line","text":""},{"location":"reference/Components/Curve/Primitive/Line/#pyhopper.Components.Curve.Primitive.Line","title":"<code>pyhopper.Components.Curve.Primitive.Line</code>","text":"<p>Line - Create a line from two points.</p>"},{"location":"reference/Components/Curve/Primitive/Line/#pyhopper.Components.Curve.Primitive.Line.Line","title":"<code>Line</code>","text":"<p>               Bases: <code>Component</code></p> <p>Create an <code>AtomicLine</code> atom between two points.</p> Source code in <code>pyhopper\\Components\\Curve\\Primitive\\Line.py</code> <pre><code>class Line(Component):\n    \"\"\"Create an ``AtomicLine`` atom between two points.\"\"\"\n\n    inputs = [\n        InputParam(\"start\", AtomicPoint, Access.ITEM),\n        InputParam(\"end\", AtomicPoint, Access.ITEM),\n    ]\n    outputs = [OutputParam(\"line\")]\n\n    def generate(self, start=None, end=None):\n        return AtomicLine(start=start, end=end)\n</code></pre>"},{"location":"reference/Components/Curve/Primitive/LineSDL/","title":"LineSDL","text":""},{"location":"reference/Components/Curve/Primitive/LineSDL/#pyhopper.Components.Curve.Primitive.LineSDL","title":"<code>pyhopper.Components.Curve.Primitive.LineSDL</code>","text":"<p>LineSDL - Create a line from start point, direction, and length.</p>"},{"location":"reference/Components/Curve/Primitive/LineSDL/#pyhopper.Components.Curve.Primitive.LineSDL.LineSDL","title":"<code>LineSDL</code>","text":"<p>               Bases: <code>Component</code></p> <p>Create an <code>AtomicLine</code> from start point, direction, and length.</p> <p>The direction vector is unitized before the line is constructed.</p> Source code in <code>pyhopper\\Components\\Curve\\Primitive\\LineSDL.py</code> <pre><code>class LineSDL(Component):\n    \"\"\"Create an ``AtomicLine`` from start point, direction, and length.\n\n    The direction vector is unitized before the line is constructed.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"start\", AtomicPoint, Access.ITEM),\n        InputParam(\"direction\", AtomicVector, Access.ITEM),\n        InputParam(\"length\", float, Access.ITEM, default=1.0),\n    ]\n    outputs = [OutputParam(\"line\")]\n\n    def generate(self, start=None, direction=None, length=1.0):\n        direction_unit = direction.unitize()\n        distance = float(length)\n        end = AtomicPoint(\n            start.x + direction_unit.x * distance,\n            start.y + direction_unit.y * distance,\n            start.z + direction_unit.z * distance,\n        )\n        return AtomicLine(start=start, end=end)\n</code></pre>"},{"location":"reference/Components/Curve/Primitive/Polygon/","title":"Polygon","text":""},{"location":"reference/Components/Curve/Primitive/Polygon/#pyhopper.Components.Curve.Primitive.Polygon","title":"<code>pyhopper.Components.Curve.Primitive.Polygon</code>","text":"<p>Polygon - Create a regular polygon from a plane, radius, and segment count.</p>"},{"location":"reference/Components/Curve/Primitive/Polygon/#pyhopper.Components.Curve.Primitive.Polygon.Polygon","title":"<code>Polygon</code>","text":"<p>               Bases: <code>Component</code></p> <p>Create a regular <code>AtomicPolyline</code> polygon from plane parameters.</p> <p>Accepts a plane, radius, segment count, and optional fillet radius, then returns the polygon outline and its perimeter length.</p> Source code in <code>pyhopper\\Components\\Curve\\Primitive\\Polygon.py</code> <pre><code>class Polygon(Component):\n    \"\"\"Create a regular ``AtomicPolyline`` polygon from plane parameters.\n\n    Accepts a plane, radius, segment count, and optional fillet radius, then\n    returns the polygon outline and its perimeter length.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"plane\", AtomicPlane, Access.ITEM, default=None, optional=True),\n        InputParam(\"radius\", float, Access.ITEM, default=1.0),\n        InputParam(\"segments\", int, Access.ITEM, default=6),\n        InputParam(\"fillet_radius\", float, Access.ITEM, default=0.0),\n    ]\n    outputs = [\n        OutputParam(\"polygon\"),\n        OutputParam(\"length\", float),\n    ]\n\n    def generate(self, plane=None, radius=1.0, segments=6, fillet_radius=0.0):\n        if plane is None:\n            plane = AtomicPlane.world_xy()\n\n        segment_count = max(3, int(segments))\n        polygon_radius = abs(float(radius))\n        _ = max(0.0, float(fillet_radius))\n\n        points = tuple(\n            _point_on_plane(\n                plane,\n                math.cos((2.0 * math.pi * index) / segment_count) * polygon_radius,\n                math.sin((2.0 * math.pi * index) / segment_count) * polygon_radius,\n            )\n            for index in range(segment_count)\n        )\n        closed = points + (points[0],)\n        return AtomicPolyline(points=closed), _polyline_length(closed)\n</code></pre>"},{"location":"reference/Components/Curve/Primitive/Rectangle/","title":"Rectangle","text":""},{"location":"reference/Components/Curve/Primitive/Rectangle/#pyhopper.Components.Curve.Primitive.Rectangle","title":"<code>pyhopper.Components.Curve.Primitive.Rectangle</code>","text":"<p>Rectangle - Create a rectangle from plane and side lengths.</p>"},{"location":"reference/Components/Curve/Primitive/Rectangle/#pyhopper.Components.Curve.Primitive.Rectangle.Rectangle","title":"<code>Rectangle</code>","text":"<p>               Bases: <code>Component</code></p> <p>Create an <code>AtomicPolyline</code> rectangle from a plane and side lengths.</p> <p>Returns the rectangle outline and its perimeter length. A non-zero radius produces a rounded rectangle approximation.</p> Source code in <code>pyhopper\\Components\\Curve\\Primitive\\Rectangle.py</code> <pre><code>class Rectangle(Component):\n    \"\"\"Create an ``AtomicPolyline`` rectangle from a plane and side lengths.\n\n    Returns the rectangle outline and its perimeter length. A non-zero radius\n    produces a rounded rectangle approximation.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"plane\", AtomicPlane, Access.ITEM, default=None, optional=True),\n        InputParam(\"x_size\", float, Access.ITEM, default=1.0),\n        InputParam(\"y_size\", float, Access.ITEM, default=1.0),\n        InputParam(\"radius\", float, Access.ITEM, default=0.0),\n    ]\n    outputs = [\n        OutputParam(\"rectangle\"),\n        OutputParam(\"length\", float),\n    ]\n\n    def generate(self, plane=None, x_size=1.0, y_size=1.0, radius=0.0):\n        if plane is None:\n            plane = AtomicPlane.world_xy()\n\n        half_x = abs(float(x_size)) / 2.0\n        half_y = abs(float(y_size)) / 2.0\n        fillet = max(0.0, min(float(radius), half_x, half_y))\n\n        if fillet == 0.0:\n            local_points = [\n                (-half_x, -half_y),\n                (half_x, -half_y),\n                (half_x, half_y),\n                (-half_x, half_y),\n                (-half_x, -half_y),\n            ]\n            perimeter = 2.0 * (abs(float(x_size)) + abs(float(y_size)))\n        else:\n            corner_segments = 6\n            local_points = []\n            corner_specs = [\n                ((half_x - fillet, half_y - fillet), 0.0, math.pi / 2.0),\n                ((-half_x + fillet, half_y - fillet), math.pi / 2.0, math.pi),\n                ((-half_x + fillet, -half_y + fillet), math.pi, 3.0 * math.pi / 2.0),\n                ((half_x - fillet, -half_y + fillet), 3.0 * math.pi / 2.0, 2.0 * math.pi),\n            ]\n\n            for (cx, cy), start_angle, end_angle in corner_specs:\n                for step in range(corner_segments + 1):\n                    t = step / corner_segments\n                    angle = start_angle + (end_angle - start_angle) * t\n                    local_points.append((\n                        cx + math.cos(angle) * fillet,\n                        cy + math.sin(angle) * fillet,\n                    ))\n\n            local_points.append(local_points[0])\n            perimeter = (\n                2.0 * (abs(float(x_size)) + abs(float(y_size)) - (4.0 * fillet))\n                + 2.0 * math.pi * fillet\n            )\n\n        points = tuple(_point_on_plane(plane, x, y) for x, y in local_points)\n        return AtomicPolyline(points=points), perimeter\n</code></pre>"},{"location":"reference/Components/Maths/Series/","title":"Series","text":""},{"location":"reference/Components/Maths/Series/#pyhopper.Components.Maths.Series","title":"<code>pyhopper.Components.Maths.Series</code>","text":"<p>Series - Generate a list of evenly spaced numbers.</p>"},{"location":"reference/Components/Maths/Series/#pyhopper.Components.Maths.Series.Series","title":"<code>Series</code>","text":"<p>               Bases: <code>Component</code></p> <p>Generate a list of evenly spaced numbers.</p> <p>The output is a single-branch tree of <code>count</code> floats starting at <code>start</code> and increasing by <code>step</code>.</p> Source code in <code>pyhopper\\Components\\Maths\\Series.py</code> <pre><code>class Series(Component):\n    \"\"\"Generate a list of evenly spaced numbers.\n\n    The output is a single-branch tree of ``count`` floats starting at\n    ``start`` and increasing by ``step``.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"start\", float, Access.ITEM, default=0.0),\n        InputParam(\"step\", float, Access.ITEM, default=1.0),\n        InputParam(\"count\", int, Access.ITEM, default=10),\n    ]\n    outputs = [OutputParam(\"series\")]\n\n    def generate(self, start=0.0, step=1.0, count=10):\n        start_value = float(start)\n        step_value = float(step)\n        count_value = int(count)\n        return [start_value + step_value * index for index in range(count_value)]\n</code></pre>"},{"location":"reference/Components/Maths/Operators/Absolute/","title":"Absolute","text":""},{"location":"reference/Components/Maths/Operators/Absolute/#pyhopper.Components.Maths.Operators.Absolute","title":"<code>pyhopper.Components.Maths.Operators.Absolute</code>","text":"<p>Absolute - Return the absolute value of a number.</p>"},{"location":"reference/Components/Maths/Operators/Absolute/#pyhopper.Components.Maths.Operators.Absolute.Absolute","title":"<code>Absolute</code>","text":"<p>               Bases: <code>Component</code></p> <p>Return the absolute value of a number.</p> <p>Accepts one number and returns its non-negative magnitude.</p> Source code in <code>pyhopper\\Components\\Maths\\Operators\\Absolute.py</code> <pre><code>class Absolute(Component):\n    \"\"\"Return the absolute value of a number.\n\n    Accepts one number and returns its non-negative magnitude.\n    \"\"\"\n\n    inputs = [InputParam(\"value\", float, Access.ITEM, default=0.0)]\n    outputs = [OutputParam(\"result\", float)]\n\n    def generate(self, value=0.0):\n        return abs(float(value))\n</code></pre>"},{"location":"reference/Components/Maths/Operators/Addition/","title":"Addition","text":""},{"location":"reference/Components/Maths/Operators/Addition/#pyhopper.Components.Maths.Operators.Addition","title":"<code>pyhopper.Components.Maths.Operators.Addition</code>","text":"<p>Addition - Add two numeric values.</p>"},{"location":"reference/Components/Maths/Operators/Addition/#pyhopper.Components.Maths.Operators.Addition.Addition","title":"<code>Addition</code>","text":"<p>               Bases: <code>Component</code></p> <p>Add two numeric values.</p> <p>Accepts two numbers and returns their sum as a single numeric output.</p> Source code in <code>pyhopper\\Components\\Maths\\Operators\\Addition.py</code> <pre><code>class Addition(Component):\n    \"\"\"Add two numeric values.\n\n    Accepts two numbers and returns their sum as a single numeric output.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"a\", float, Access.ITEM, default=0.0),\n        InputParam(\"b\", float, Access.ITEM, default=0.0),\n    ]\n    outputs = [OutputParam(\"result\", float)]\n\n    def generate(self, a=0.0, b=0.0):\n        return float(a) + float(b)\n</code></pre>"},{"location":"reference/Components/Maths/Operators/Division/","title":"Division","text":""},{"location":"reference/Components/Maths/Operators/Division/#pyhopper.Components.Maths.Operators.Division","title":"<code>pyhopper.Components.Maths.Operators.Division</code>","text":"<p>Division - Divide one numeric value by another.</p>"},{"location":"reference/Components/Maths/Operators/Division/#pyhopper.Components.Maths.Operators.Division.Division","title":"<code>Division</code>","text":"<p>               Bases: <code>Component</code></p> <p>Divide one numeric value by another.</p> <p>Accepts two numbers and returns <code>a / b</code> as a single numeric output.</p> Source code in <code>pyhopper\\Components\\Maths\\Operators\\Division.py</code> <pre><code>class Division(Component):\n    \"\"\"Divide one numeric value by another.\n\n    Accepts two numbers and returns ``a / b`` as a single numeric output.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"a\", float, Access.ITEM, default=1.0),\n        InputParam(\"b\", float, Access.ITEM, default=1.0),\n    ]\n    outputs = [OutputParam(\"result\", float)]\n\n    def generate(self, a=1.0, b=1.0):\n        return float(a) / float(b)\n</code></pre>"},{"location":"reference/Components/Maths/Operators/Factorial/","title":"Factorial","text":""},{"location":"reference/Components/Maths/Operators/Factorial/#pyhopper.Components.Maths.Operators.Factorial","title":"<code>pyhopper.Components.Maths.Operators.Factorial</code>","text":"<p>Factorial - Compute the factorial of an integer.</p>"},{"location":"reference/Components/Maths/Operators/Factorial/#pyhopper.Components.Maths.Operators.Factorial.Factorial","title":"<code>Factorial</code>","text":"<p>               Bases: <code>Component</code></p> <p>Compute the factorial of an integer.</p> <p>Accepts one integer and returns <code>n!</code> as a single integer output.</p> Source code in <code>pyhopper\\Components\\Maths\\Operators\\Factorial.py</code> <pre><code>class Factorial(Component):\n    \"\"\"Compute the factorial of an integer.\n\n    Accepts one integer and returns ``n!`` as a single integer output.\n    \"\"\"\n\n    inputs = [InputParam(\"n\", int, Access.ITEM, default=0)]\n    outputs = [OutputParam(\"result\", int)]\n\n    def generate(self, n=0):\n        return math.factorial(int(n))\n</code></pre>"},{"location":"reference/Components/Maths/Operators/IntegerDivision/","title":"IntegerDivision","text":""},{"location":"reference/Components/Maths/Operators/IntegerDivision/#pyhopper.Components.Maths.Operators.IntegerDivision","title":"<code>pyhopper.Components.Maths.Operators.IntegerDivision</code>","text":"<p>IntegerDivision - Divide two integers using floor division.</p>"},{"location":"reference/Components/Maths/Operators/IntegerDivision/#pyhopper.Components.Maths.Operators.IntegerDivision.IntegerDivision","title":"<code>IntegerDivision</code>","text":"<p>               Bases: <code>Component</code></p> <p>Divide two integers using floor division.</p> <p>Accepts two integers and returns <code>a // b</code> as a single integer output.</p> Source code in <code>pyhopper\\Components\\Maths\\Operators\\IntegerDivision.py</code> <pre><code>class IntegerDivision(Component):\n    \"\"\"Divide two integers using floor division.\n\n    Accepts two integers and returns ``a // b`` as a single integer output.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"a\", int, Access.ITEM, default=1),\n        InputParam(\"b\", int, Access.ITEM, default=1),\n    ]\n    outputs = [OutputParam(\"result\", int)]\n\n    def generate(self, a=1, b=1):\n        return int(a) // int(b)\n</code></pre>"},{"location":"reference/Components/Maths/Operators/Modulus/","title":"Modulus","text":""},{"location":"reference/Components/Maths/Operators/Modulus/#pyhopper.Components.Maths.Operators.Modulus","title":"<code>pyhopper.Components.Maths.Operators.Modulus</code>","text":"<p>Modulus - Compute the remainder of numeric division.</p>"},{"location":"reference/Components/Maths/Operators/Modulus/#pyhopper.Components.Maths.Operators.Modulus.Modulus","title":"<code>Modulus</code>","text":"<p>               Bases: <code>Component</code></p> <p>Compute the remainder of numeric division.</p> <p>Accepts two numbers and returns <code>a % b</code> as a single numeric output.</p> Source code in <code>pyhopper\\Components\\Maths\\Operators\\Modulus.py</code> <pre><code>class Modulus(Component):\n    \"\"\"Compute the remainder of numeric division.\n\n    Accepts two numbers and returns ``a % b`` as a single numeric output.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"a\", float, Access.ITEM, default=0.0),\n        InputParam(\"b\", float, Access.ITEM, default=1.0),\n    ]\n    outputs = [OutputParam(\"result\", float)]\n\n    def generate(self, a=0.0, b=1.0):\n        return float(a) % float(b)\n</code></pre>"},{"location":"reference/Components/Maths/Operators/Multiplication/","title":"Multiplication","text":""},{"location":"reference/Components/Maths/Operators/Multiplication/#pyhopper.Components.Maths.Operators.Multiplication","title":"<code>pyhopper.Components.Maths.Operators.Multiplication</code>","text":"<p>Multiplication - Multiply two numeric values.</p>"},{"location":"reference/Components/Maths/Operators/Multiplication/#pyhopper.Components.Maths.Operators.Multiplication.Multiplication","title":"<code>Multiplication</code>","text":"<p>               Bases: <code>Component</code></p> <p>Multiply two numeric values.</p> <p>Accepts two numbers and returns their product as a single numeric output.</p> Source code in <code>pyhopper\\Components\\Maths\\Operators\\Multiplication.py</code> <pre><code>class Multiplication(Component):\n    \"\"\"Multiply two numeric values.\n\n    Accepts two numbers and returns their product as a single numeric output.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"a\", float, Access.ITEM, default=1.0),\n        InputParam(\"b\", float, Access.ITEM, default=1.0),\n    ]\n    outputs = [OutputParam(\"result\", float)]\n\n    def generate(self, a=1.0, b=1.0):\n        return float(a) * float(b)\n</code></pre>"},{"location":"reference/Components/Maths/Operators/Negative/","title":"Negative","text":""},{"location":"reference/Components/Maths/Operators/Negative/#pyhopper.Components.Maths.Operators.Negative","title":"<code>pyhopper.Components.Maths.Operators.Negative</code>","text":"<p>Negative - Negate a numeric value.</p>"},{"location":"reference/Components/Maths/Operators/Negative/#pyhopper.Components.Maths.Operators.Negative.Negative","title":"<code>Negative</code>","text":"<p>               Bases: <code>Component</code></p> <p>Negate a numeric value.</p> <p>Accepts one number and returns its additive inverse.</p> Source code in <code>pyhopper\\Components\\Maths\\Operators\\Negative.py</code> <pre><code>class Negative(Component):\n    \"\"\"Negate a numeric value.\n\n    Accepts one number and returns its additive inverse.\n    \"\"\"\n\n    inputs = [InputParam(\"value\", float, Access.ITEM, default=0.0)]\n    outputs = [OutputParam(\"result\", float)]\n\n    def generate(self, value=0.0):\n        return -float(value)\n</code></pre>"},{"location":"reference/Components/Maths/Operators/Power/","title":"Power","text":""},{"location":"reference/Components/Maths/Operators/Power/#pyhopper.Components.Maths.Operators.Power","title":"<code>pyhopper.Components.Maths.Operators.Power</code>","text":"<p>Power - Raise a numeric value to an exponent.</p>"},{"location":"reference/Components/Maths/Operators/Power/#pyhopper.Components.Maths.Operators.Power.Power","title":"<code>Power</code>","text":"<p>               Bases: <code>Component</code></p> <p>Raise a numeric value to an exponent.</p> <p>Accepts a base and an exponent and returns <code>base ** exponent</code>.</p> Source code in <code>pyhopper\\Components\\Maths\\Operators\\Power.py</code> <pre><code>class Power(Component):\n    \"\"\"Raise a numeric value to an exponent.\n\n    Accepts a base and an exponent and returns ``base ** exponent``.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"base\", float, Access.ITEM, default=1.0),\n        InputParam(\"exponent\", float, Access.ITEM, default=2.0),\n    ]\n    outputs = [OutputParam(\"result\", float)]\n\n    def generate(self, base=1.0, exponent=2.0):\n        return float(base) ** float(exponent)\n</code></pre>"},{"location":"reference/Components/Maths/Operators/Subtraction/","title":"Subtraction","text":""},{"location":"reference/Components/Maths/Operators/Subtraction/#pyhopper.Components.Maths.Operators.Subtraction","title":"<code>pyhopper.Components.Maths.Operators.Subtraction</code>","text":"<p>Subtraction - Subtract one numeric value from another.</p>"},{"location":"reference/Components/Maths/Operators/Subtraction/#pyhopper.Components.Maths.Operators.Subtraction.Subtraction","title":"<code>Subtraction</code>","text":"<p>               Bases: <code>Component</code></p> <p>Subtract one numeric value from another.</p> <p>Accepts two numbers and returns <code>a - b</code> as a single numeric output.</p> Source code in <code>pyhopper\\Components\\Maths\\Operators\\Subtraction.py</code> <pre><code>class Subtraction(Component):\n    \"\"\"Subtract one numeric value from another.\n\n    Accepts two numbers and returns ``a - b`` as a single numeric output.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"a\", float, Access.ITEM, default=0.0),\n        InputParam(\"b\", float, Access.ITEM, default=0.0),\n    ]\n    outputs = [OutputParam(\"result\", float)]\n\n    def generate(self, a=0.0, b=0.0):\n        return float(a) - float(b)\n</code></pre>"},{"location":"reference/Components/Params/Input/NumberSlider/","title":"NumberSlider","text":""},{"location":"reference/Components/Params/Input/NumberSlider/#pyhopper.Components.Params.Input.NumberSlider","title":"<code>pyhopper.Components.Params.Input.NumberSlider</code>","text":"<p>NumberSlider - Provide a numeric parameter with a slider-style frontend preset.</p>"},{"location":"reference/Components/Params/Input/NumberSlider/#pyhopper.Components.Params.Input.NumberSlider.NumberSlider","title":"<code>NumberSlider</code>","text":"<p>               Bases: <code>Component</code></p> <p>Provide a numeric parameter as a zero-input component.</p> <p>This component follows the standard :class:<code>pyhopper.Core.Component.Component</code> contract: it declares no inputs, exposes one output named <code>value</code>, and returns a single float from <code>generate()</code>. Tree coercion, branch handling, path propagation, and result wrapping are all inherited unchanged from the base class.</p> <p>The optional <code>frontend_preset</code> metadata allows the web canvas to render this parameter as an interactive slider. That metadata is declarative only; it does not replace the normal component solve model.</p> Source code in <code>pyhopper\\Components\\Params\\Input\\NumberSlider.py</code> <pre><code>class NumberSlider(Component):\n    \"\"\"Provide a numeric parameter as a zero-input component.\n\n    This component follows the standard :class:`pyhopper.Core.Component.Component`\n    contract: it declares no inputs, exposes one output named ``value``, and\n    returns a single float from ``generate()``. Tree coercion, branch handling,\n    path propagation, and result wrapping are all inherited unchanged from the\n    base class.\n\n    The optional ``frontend_preset`` metadata allows the web canvas to render\n    this parameter as an interactive slider. That metadata is declarative only;\n    it does not replace the normal component solve model.\n    \"\"\"\n\n    DEFAULT_MIN = 0.0\n    DEFAULT_MAX = 1.0\n    DEFAULT_STEP = 0.01\n    DEFAULT_DECIMALS = 2\n    DEFAULT_VALUE = 0.5\n\n    inputs = []\n    outputs = [OutputParam(\"value\", float)]\n    frontend_preset = \"number-slider\"\n    frontend_config = {\n        \"min\": DEFAULT_MIN,\n        \"max\": DEFAULT_MAX,\n        \"step\": DEFAULT_STEP,\n        \"decimals\": DEFAULT_DECIMALS,\n        \"value\": DEFAULT_VALUE,\n    }\n\n    def generate(self) -&gt; float:\n        \"\"\"Return the component's default numeric value.\"\"\"\n        return float(self.DEFAULT_VALUE)\n</code></pre>"},{"location":"reference/Components/Params/Input/NumberSlider/#pyhopper.Components.Params.Input.NumberSlider.NumberSlider.generate","title":"<code>generate()</code>","text":"<p>Return the component's default numeric value.</p> Source code in <code>pyhopper\\Components\\Params\\Input\\NumberSlider.py</code> <pre><code>def generate(self) -&gt; float:\n    \"\"\"Return the component's default numeric value.\"\"\"\n    return float(self.DEFAULT_VALUE)\n</code></pre>"},{"location":"reference/Components/Sets/Tree/Merge/","title":"Merge","text":""},{"location":"reference/Components/Sets/Tree/Merge/#pyhopper.Components.Sets.Tree.Merge","title":"<code>pyhopper.Components.Sets.Tree.Merge</code>","text":"<p>Merge - Merge multiple DataTrees into one (Sets &gt; Tree).</p>"},{"location":"reference/Components/Sets/Tree/Merge/#pyhopper.Components.Sets.Tree.Merge.Merge","title":"<code>Merge</code>","text":"<p>               Bases: <code>Component</code></p> <p>Merge any number of DataTrees into a single DataTree.</p> <p>Branches on the same path have their items concatenated, while unique branches are carried over unchanged. This mirrors Grasshopper's Merge component and accepts a variable number of positional arguments.</p> Source code in <code>pyhopper\\Components\\Sets\\Tree\\Merge.py</code> <pre><code>class Merge(Component):\n    \"\"\"Merge any number of DataTrees into a single DataTree.\n\n    Branches on the same path have their items concatenated, while unique\n    branches are carried over unchanged. This mirrors Grasshopper's Merge\n    component and accepts a variable number of positional arguments.\n    \"\"\"\n\n    inputs = [InputParam(\"data\", None, Access.TREE, optional=True)]\n    outputs = [OutputParam(\"result\")]\n    variadic_inputs = True\n\n    def __new__(cls, *args) -&gt; ComponentResult:  # type: ignore[override]\n        trees = [DataTree.coerce(a) for a in args]\n        merged = DataTree.merge(*trees)\n        return ComponentResult(merged, {\"result\": merged})\n</code></pre>"},{"location":"reference/Components/Vector/Plane/ConstructPlane/","title":"ConstructPlane","text":""},{"location":"reference/Components/Vector/Plane/ConstructPlane/#pyhopper.Components.Vector.Plane.ConstructPlane","title":"<code>pyhopper.Components.Vector.Plane.ConstructPlane</code>","text":"<p>ConstructPlane - Create a plane from origin, x-axis, and y-axis.</p>"},{"location":"reference/Components/Vector/Plane/ConstructPlane/#pyhopper.Components.Vector.Plane.ConstructPlane.ConstructPlane","title":"<code>ConstructPlane</code>","text":"<p>               Bases: <code>Component</code></p> <p>Create an <code>AtomicPlane</code> from origin, x-axis, and y-axis vectors.</p> <p>Mirrors Grasshopper's Construct Plane component: the plane is defined by an origin point and two in-plane axis vectors.</p> Source code in <code>pyhopper\\Components\\Vector\\Plane\\ConstructPlane.py</code> <pre><code>class ConstructPlane(Component):\n    \"\"\"Create an ``AtomicPlane`` from origin, x-axis, and y-axis vectors.\n\n    Mirrors Grasshopper's Construct Plane component: the plane is defined by\n    an origin point and two in-plane axis vectors.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"origin\", AtomicPoint, Access.ITEM, default=AtomicPoint.origin()),\n        InputParam(\"x_axis\", AtomicVector, Access.ITEM, default=AtomicVector.unit_x()),\n        InputParam(\"y_axis\", AtomicVector, Access.ITEM, default=AtomicVector.unit_y()),\n    ]\n    outputs = [OutputParam(\"plane\", AtomicPlane)]\n\n    def generate(self, origin=None, x_axis=None, y_axis=None):\n        if origin is None:\n            origin = AtomicPoint.origin()\n\n        if x_axis is None or x_axis.length == 0:\n            x_axis = AtomicVector.unit_x()\n        else:\n            x_axis = x_axis.unitize()\n\n        if y_axis is None or y_axis.length == 0:\n            y_axis = AtomicVector.unit_y()\n        else:\n            y_axis = y_axis.unitize()\n\n        normal = AtomicVector(\n            x_axis.y * y_axis.z - x_axis.z * y_axis.y,\n            x_axis.z * y_axis.x - x_axis.x * y_axis.z,\n            x_axis.x * y_axis.y - x_axis.y * y_axis.x,\n        ).unitize()\n\n        if normal.length == 0:\n            normal = AtomicVector.unit_z()\n            x_axis = AtomicVector.unit_x()\n        else:\n            x_axis = AtomicVector(\n                y_axis.y * normal.z - y_axis.z * normal.y,\n                y_axis.z * normal.x - y_axis.x * normal.z,\n                y_axis.x * normal.y - y_axis.y * normal.x,\n            ).unitize()\n\n        return AtomicPlane(origin=origin, normal=normal, x_axis=x_axis)\n</code></pre>"},{"location":"reference/Components/Vector/Vector/Amplitude/","title":"Amplitude","text":""},{"location":"reference/Components/Vector/Vector/Amplitude/#pyhopper.Components.Vector.Vector.Amplitude","title":"<code>pyhopper.Components.Vector.Vector.Amplitude</code>","text":"<p>Amplitude - Set the amplitude of a vector.</p>"},{"location":"reference/Components/Vector/Vector/Amplitude/#pyhopper.Components.Vector.Vector.Amplitude.Amplitude","title":"<code>Amplitude</code>","text":"<p>               Bases: <code>Component</code></p> <p>Set a vector's amplitude while preserving its direction.</p> Source code in <code>pyhopper\\Components\\Vector\\Vector\\Amplitude.py</code> <pre><code>class Amplitude(Component):\n    \"\"\"Set a vector's amplitude while preserving its direction.\"\"\"\n\n    inputs = [\n        InputParam(\"vector\", AtomicVector, Access.ITEM),\n        InputParam(\"amplitude\", float, Access.ITEM, default=1.0),\n    ]\n    outputs = [OutputParam(\"vector\", AtomicVector)]\n\n    def generate(self, vector=None, amplitude=1.0):\n        direction = vector.unitize()\n        scale = float(amplitude)\n        return AtomicVector(direction.x * scale, direction.y * scale, direction.z * scale)\n</code></pre>"},{"location":"reference/Components/Vector/Vector/Angle/","title":"Angle","text":""},{"location":"reference/Components/Vector/Vector/Angle/#pyhopper.Components.Vector.Vector.Angle","title":"<code>pyhopper.Components.Vector.Vector.Angle</code>","text":"<p>Angle - Compute the angle between two vectors.</p>"},{"location":"reference/Components/Vector/Vector/Angle/#pyhopper.Components.Vector.Vector.Angle.Angle","title":"<code>Angle</code>","text":"<p>               Bases: <code>Component</code></p> <p>Compute the angle and reflex angle between two vectors.</p> <p>When a plane is supplied, the vectors are projected to that plane before the angles are measured.</p> Source code in <code>pyhopper\\Components\\Vector\\Vector\\Angle.py</code> <pre><code>class Angle(Component):\n    \"\"\"Compute the angle and reflex angle between two vectors.\n\n    When a plane is supplied, the vectors are projected to that plane before\n    the angles are measured.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"vector_a\", AtomicVector, Access.ITEM),\n        InputParam(\"vector_b\", AtomicVector, Access.ITEM),\n        InputParam(\"plane\", AtomicPlane, Access.ITEM, default=None, optional=True),\n    ]\n    outputs = [\n        OutputParam(\"angle\", float),\n        OutputParam(\"reflex\", float),\n    ]\n\n    def generate(self, vector_a=None, vector_b=None, plane=None):\n        if plane is not None:\n            vector_a = _project_onto_plane(vector_a, plane)\n            vector_b = _project_onto_plane(vector_b, plane)\n\n        a = vector_a.unitize()\n        b = vector_b.unitize()\n        cosine = max(-1.0, min(1.0, _dot_product(a, b)))\n        angle = math.acos(cosine)\n        return angle, (2.0 * math.pi) - angle\n</code></pre>"},{"location":"reference/Components/Vector/Vector/CrossProduct/","title":"CrossProduct","text":""},{"location":"reference/Components/Vector/Vector/CrossProduct/#pyhopper.Components.Vector.Vector.CrossProduct","title":"<code>pyhopper.Components.Vector.Vector.CrossProduct</code>","text":"<p>CrossProduct - Compute the cross product of two vectors.</p>"},{"location":"reference/Components/Vector/Vector/CrossProduct/#pyhopper.Components.Vector.Vector.CrossProduct.CrossProduct","title":"<code>CrossProduct</code>","text":"<p>               Bases: <code>Component</code></p> <p>Compute the cross product of two vectors.</p> <p>When <code>unitize</code> is true, both vectors are unitized before the cross product is evaluated.</p> Source code in <code>pyhopper\\Components\\Vector\\Vector\\CrossProduct.py</code> <pre><code>class CrossProduct(Component):\n    \"\"\"Compute the cross product of two vectors.\n\n    When ``unitize`` is true, both vectors are unitized before the cross\n    product is evaluated.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"vector_a\", AtomicVector, Access.ITEM),\n        InputParam(\"vector_b\", AtomicVector, Access.ITEM),\n        InputParam(\"unitize\", bool, Access.ITEM, default=False),\n    ]\n    outputs = [\n        OutputParam(\"vector\", AtomicVector),\n        OutputParam(\"length\", float),\n    ]\n\n    def generate(self, vector_a=None, vector_b=None, unitize=False):\n        if unitize:\n            vector_a = vector_a.unitize()\n            vector_b = vector_b.unitize()\n\n        vector = AtomicVector(\n            vector_a.y * vector_b.z - vector_a.z * vector_b.y,\n            vector_a.z * vector_b.x - vector_a.x * vector_b.z,\n            vector_a.x * vector_b.y - vector_a.y * vector_b.x,\n        )\n        return vector, vector.length\n</code></pre>"},{"location":"reference/Components/Vector/Vector/DeconstructVector/","title":"DeconstructVector","text":""},{"location":"reference/Components/Vector/Vector/DeconstructVector/#pyhopper.Components.Vector.Vector.DeconstructVector","title":"<code>pyhopper.Components.Vector.Vector.DeconstructVector</code>","text":"<p>DeconstructVector - Split a vector into its XYZ components.</p>"},{"location":"reference/Components/Vector/Vector/DeconstructVector/#pyhopper.Components.Vector.Vector.DeconstructVector.DeconstructVector","title":"<code>DeconstructVector</code>","text":"<p>               Bases: <code>Component</code></p> <p>Split an <code>AtomicVector</code> into X, Y, and Z scalar components.</p> Source code in <code>pyhopper\\Components\\Vector\\Vector\\DeconstructVector.py</code> <pre><code>class DeconstructVector(Component):\n    \"\"\"Split an ``AtomicVector`` into X, Y, and Z scalar components.\"\"\"\n\n    inputs = [InputParam(\"vector\", AtomicVector, Access.ITEM)]\n    outputs = [\n        OutputParam(\"x_component\", float),\n        OutputParam(\"y_component\", float),\n        OutputParam(\"z_component\", float),\n    ]\n\n    def generate(self, vector=None):\n        return float(vector.x), float(vector.y), float(vector.z)\n</code></pre>"},{"location":"reference/Components/Vector/Vector/DotProduct/","title":"DotProduct","text":""},{"location":"reference/Components/Vector/Vector/DotProduct/#pyhopper.Components.Vector.Vector.DotProduct","title":"<code>pyhopper.Components.Vector.Vector.DotProduct</code>","text":"<p>DotProduct - Compute the dot product of two vectors.</p>"},{"location":"reference/Components/Vector/Vector/DotProduct/#pyhopper.Components.Vector.Vector.DotProduct.DotProduct","title":"<code>DotProduct</code>","text":"<p>               Bases: <code>Component</code></p> <p>Compute the dot product of two vectors.</p> <p>When <code>unitize</code> is true, both vectors are unitized before the dot product is evaluated.</p> Source code in <code>pyhopper\\Components\\Vector\\Vector\\DotProduct.py</code> <pre><code>class DotProduct(Component):\n    \"\"\"Compute the dot product of two vectors.\n\n    When ``unitize`` is true, both vectors are unitized before the dot product\n    is evaluated.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"vector_a\", AtomicVector, Access.ITEM),\n        InputParam(\"vector_b\", AtomicVector, Access.ITEM),\n        InputParam(\"unitize\", bool, Access.ITEM, default=False),\n    ]\n    outputs = [OutputParam(\"dot_product\", float)]\n\n    def generate(self, vector_a=None, vector_b=None, unitize=False):\n        if unitize:\n            vector_a = vector_a.unitize()\n            vector_b = vector_b.unitize()\n        return vector_a.x * vector_b.x + vector_a.y * vector_b.y + vector_a.z * vector_b.z\n</code></pre>"},{"location":"reference/Components/Vector/Vector/Reverse/","title":"Reverse","text":""},{"location":"reference/Components/Vector/Vector/Reverse/#pyhopper.Components.Vector.Vector.Reverse","title":"<code>pyhopper.Components.Vector.Vector.Reverse</code>","text":"<p>Reverse - Reverse the direction of a vector.</p>"},{"location":"reference/Components/Vector/Vector/Reverse/#pyhopper.Components.Vector.Vector.Reverse.Reverse","title":"<code>Reverse</code>","text":"<p>               Bases: <code>Component</code></p> <p>Reverse the direction of an <code>AtomicVector</code>.</p> Source code in <code>pyhopper\\Components\\Vector\\Vector\\Reverse.py</code> <pre><code>class Reverse(Component):\n    \"\"\"Reverse the direction of an ``AtomicVector``.\"\"\"\n\n    inputs = [InputParam(\"vector\", AtomicVector, Access.ITEM)]\n    outputs = [OutputParam(\"vector\", AtomicVector)]\n\n    def generate(self, vector=None):\n        return AtomicVector(-vector.x, -vector.y, -vector.z)\n</code></pre>"},{"location":"reference/Components/Vector/Vector/Rotate/","title":"Rotate","text":""},{"location":"reference/Components/Vector/Vector/Rotate/#pyhopper.Components.Vector.Vector.Rotate","title":"<code>pyhopper.Components.Vector.Vector.Rotate</code>","text":"<p>Rotate - Rotate a vector around an axis.</p>"},{"location":"reference/Components/Vector/Vector/Rotate/#pyhopper.Components.Vector.Vector.Rotate.Rotate","title":"<code>Rotate</code>","text":"<p>               Bases: <code>Component</code></p> <p>Rotate a vector around an axis by an angle in radians.</p> Source code in <code>pyhopper\\Components\\Vector\\Vector\\Rotate.py</code> <pre><code>class Rotate(Component):\n    \"\"\"Rotate a vector around an axis by an angle in radians.\"\"\"\n\n    inputs = [\n        InputParam(\"vector\", AtomicVector, Access.ITEM),\n        InputParam(\"axis\", AtomicVector, Access.ITEM),\n        InputParam(\"angle\", float, Access.ITEM, default=0.0),\n    ]\n    outputs = [OutputParam(\"vector\", AtomicVector)]\n\n    def generate(self, vector=None, axis=None, angle=0.0):\n        unit_axis = axis.unitize()\n        angle_value = float(angle)\n        cos_angle = math.cos(angle_value)\n        sin_angle = math.sin(angle_value)\n        dot = (\n            unit_axis.x * vector.x +\n            unit_axis.y * vector.y +\n            unit_axis.z * vector.z\n        )\n\n        return AtomicVector(\n            vector.x * cos_angle + (unit_axis.y * vector.z - unit_axis.z * vector.y) * sin_angle + unit_axis.x * dot * (1.0 - cos_angle),\n            vector.y * cos_angle + (unit_axis.z * vector.x - unit_axis.x * vector.z) * sin_angle + unit_axis.y * dot * (1.0 - cos_angle),\n            vector.z * cos_angle + (unit_axis.x * vector.y - unit_axis.y * vector.x) * sin_angle + unit_axis.z * dot * (1.0 - cos_angle),\n        )\n</code></pre>"},{"location":"reference/Components/Vector/Vector/UnitX/","title":"UnitX","text":""},{"location":"reference/Components/Vector/Vector/UnitX/#pyhopper.Components.Vector.Vector.UnitX","title":"<code>pyhopper.Components.Vector.Vector.UnitX</code>","text":"<p>UnitX - Returns a unit vector along the X axis.</p>"},{"location":"reference/Components/Vector/Vector/UnitX/#pyhopper.Components.Vector.Vector.UnitX.UnitX","title":"<code>UnitX</code>","text":"<p>               Bases: <code>Component</code></p> <p>Return a unit vector along the world X axis <code>(1, 0, 0)</code>.</p> Source code in <code>pyhopper\\Components\\Vector\\Vector\\UnitX.py</code> <pre><code>class UnitX(Component):\n    \"\"\"Return a unit vector along the world X axis ``(1, 0, 0)``.\"\"\"\n\n    inputs = []\n    outputs = [OutputParam(\"vector\")]\n\n    def generate(self):\n        return AtomicVector.unit_x()\n</code></pre>"},{"location":"reference/Components/Vector/Vector/UnitY/","title":"UnitY","text":""},{"location":"reference/Components/Vector/Vector/UnitY/#pyhopper.Components.Vector.Vector.UnitY","title":"<code>pyhopper.Components.Vector.Vector.UnitY</code>","text":"<p>UnitY - Returns a unit vector along the Y axis.</p>"},{"location":"reference/Components/Vector/Vector/UnitY/#pyhopper.Components.Vector.Vector.UnitY.UnitY","title":"<code>UnitY</code>","text":"<p>               Bases: <code>Component</code></p> <p>Return a unit vector along the world Y axis <code>(0, 1, 0)</code>.</p> Source code in <code>pyhopper\\Components\\Vector\\Vector\\UnitY.py</code> <pre><code>class UnitY(Component):\n    \"\"\"Return a unit vector along the world Y axis ``(0, 1, 0)``.\"\"\"\n\n    inputs = []\n    outputs = [OutputParam(\"vector\")]\n\n    def generate(self):\n        return AtomicVector.unit_y()\n</code></pre>"},{"location":"reference/Components/Vector/Vector/UnitZ/","title":"UnitZ","text":""},{"location":"reference/Components/Vector/Vector/UnitZ/#pyhopper.Components.Vector.Vector.UnitZ","title":"<code>pyhopper.Components.Vector.Vector.UnitZ</code>","text":"<p>UnitZ - Returns a unit vector along the Z axis.</p>"},{"location":"reference/Components/Vector/Vector/UnitZ/#pyhopper.Components.Vector.Vector.UnitZ.UnitZ","title":"<code>UnitZ</code>","text":"<p>               Bases: <code>Component</code></p> <p>Return a unit vector along the world Z axis <code>(0, 0, 1)</code>.</p> Source code in <code>pyhopper\\Components\\Vector\\Vector\\UnitZ.py</code> <pre><code>class UnitZ(Component):\n    \"\"\"Return a unit vector along the world Z axis ``(0, 0, 1)``.\"\"\"\n\n    inputs = []\n    outputs = [OutputParam(\"vector\")]\n\n    def generate(self):\n        return AtomicVector.unit_z()\n</code></pre>"},{"location":"reference/Components/Vector/Vector/UnitZ/#pyhopper.Components.Vector.Vector.UnitZ.UnitX","title":"<code>UnitX</code>","text":"<p>               Bases: <code>Component</code></p> <p>Return a unit vector along the world X axis <code>(1, 0, 0)</code>.</p> Source code in <code>pyhopper\\Components\\Vector\\Vector\\UnitZ.py</code> <pre><code>class UnitX(Component):\n    \"\"\"Return a unit vector along the world X axis ``(1, 0, 0)``.\"\"\"\n\n    inputs = []\n    outputs = [OutputParam(\"vector\")]\n\n    def generate(self):\n        return AtomicVector.unit_x()\n</code></pre>"},{"location":"reference/Components/Vector/Vector/UnitZ/#pyhopper.Components.Vector.Vector.UnitZ.UnitY","title":"<code>UnitY</code>","text":"<p>               Bases: <code>Component</code></p> <p>Return a unit vector along the world Y axis <code>(0, 1, 0)</code>.</p> Source code in <code>pyhopper\\Components\\Vector\\Vector\\UnitZ.py</code> <pre><code>class UnitY(Component):\n    \"\"\"Return a unit vector along the world Y axis ``(0, 1, 0)``.\"\"\"\n\n    inputs = []\n    outputs = [OutputParam(\"vector\")]\n\n    def generate(self):\n        return AtomicVector.unit_y()\n</code></pre>"},{"location":"reference/Components/Vector/Vector/Vector2Pt/","title":"Vector2Pt","text":""},{"location":"reference/Components/Vector/Vector/Vector2Pt/#pyhopper.Components.Vector.Vector.Vector2Pt","title":"<code>pyhopper.Components.Vector.Vector.Vector2Pt</code>","text":"<p>Vector2Pt - Create a vector from two points.</p>"},{"location":"reference/Components/Vector/Vector/Vector2Pt/#pyhopper.Components.Vector.Vector.Vector2Pt.Vector2Pt","title":"<code>Vector2Pt</code>","text":"<p>               Bases: <code>Component</code></p> <p>Create an <code>AtomicVector</code> from point A to point B.</p> <p>Optionally unitizes the output vector while also reporting the original point-to-point distance.</p> Source code in <code>pyhopper\\Components\\Vector\\Vector\\Vector2Pt.py</code> <pre><code>class Vector2Pt(Component):\n    \"\"\"Create an ``AtomicVector`` from point A to point B.\n\n    Optionally unitizes the output vector while also reporting the original\n    point-to-point distance.\n    \"\"\"\n\n    inputs = [\n        InputParam(\"point_a\", AtomicPoint, Access.ITEM),\n        InputParam(\"point_b\", AtomicPoint, Access.ITEM),\n        InputParam(\"unitize\", bool, Access.ITEM, default=False),\n    ]\n    outputs = [\n        OutputParam(\"vector\", AtomicVector),\n        OutputParam(\"length\", float),\n    ]\n\n    def generate(self, point_a=None, point_b=None, unitize=False):\n        vector = AtomicVector(\n            point_b.x - point_a.x,\n            point_b.y - point_a.y,\n            point_b.z - point_a.z,\n        )\n        length = vector.length\n        if unitize:\n            vector = vector.unitize()\n        return vector, length\n</code></pre>"},{"location":"reference/Components/Vector/Vector/VectorLength/","title":"VectorLength","text":""},{"location":"reference/Components/Vector/Vector/VectorLength/#pyhopper.Components.Vector.Vector.VectorLength","title":"<code>pyhopper.Components.Vector.Vector.VectorLength</code>","text":"<p>VectorLength - Measure the length of a vector.</p>"},{"location":"reference/Components/Vector/Vector/VectorLength/#pyhopper.Components.Vector.Vector.VectorLength.VectorLength","title":"<code>VectorLength</code>","text":"<p>               Bases: <code>Component</code></p> <p>Measure the Euclidean length of an <code>AtomicVector</code>.</p> Source code in <code>pyhopper\\Components\\Vector\\Vector\\VectorLength.py</code> <pre><code>class VectorLength(Component):\n    \"\"\"Measure the Euclidean length of an ``AtomicVector``.\"\"\"\n\n    inputs = [InputParam(\"vector\", AtomicVector, Access.ITEM)]\n    outputs = [OutputParam(\"length\", float)]\n\n    def generate(self, vector=None):\n        return vector.length\n</code></pre>"},{"location":"reference/Components/Vector/Vector/VectorXYZ/","title":"VectorXYZ","text":""},{"location":"reference/Components/Vector/Vector/VectorXYZ/#pyhopper.Components.Vector.Vector.VectorXYZ","title":"<code>pyhopper.Components.Vector.Vector.VectorXYZ</code>","text":"<p>VectorXYZ - Construct a vector from XYZ scalar components.</p>"},{"location":"reference/Components/Vector/Vector/VectorXYZ/#pyhopper.Components.Vector.Vector.VectorXYZ.VectorXYZ","title":"<code>VectorXYZ</code>","text":"<p>               Bases: <code>Component</code></p> <p>Construct an <code>AtomicVector</code> from X, Y, and Z scalar components.</p> Source code in <code>pyhopper\\Components\\Vector\\Vector\\VectorXYZ.py</code> <pre><code>class VectorXYZ(Component):\n    \"\"\"Construct an ``AtomicVector`` from X, Y, and Z scalar components.\"\"\"\n\n    inputs = [\n        InputParam(\"x_component\", float, Access.ITEM, default=0.0),\n        InputParam(\"y_component\", float, Access.ITEM, default=0.0),\n        InputParam(\"z_component\", float, Access.ITEM, default=0.0),\n    ]\n    outputs = [\n        OutputParam(\"vector\", AtomicVector),\n        OutputParam(\"length\", float),\n    ]\n\n    def generate(self, x_component=0.0, y_component=0.0, z_component=0.0):\n        vector = AtomicVector(float(x_component), float(y_component), float(z_component))\n        return vector, vector.length\n</code></pre>"},{"location":"reference/Core/Atoms/","title":"Atoms","text":""},{"location":"reference/Core/Atoms/#pyhopper.Core.Atoms","title":"<code>pyhopper.Core.Atoms</code>","text":"<p>Atoms - Immutable geometric primitives for pyhopper.</p> <p>Atoms are the indivisible data units that flow through DataTrees. They are pure data containers with no behavior \u2014 all operations live in Components and Adapters.</p>"},{"location":"reference/Core/Atoms/#pyhopper.Core.Atoms.Atom","title":"<code>Atom</code>","text":"<p>Base class for all geometric primitives.</p> <p>Subclasses must be frozen dataclasses and define atom_type as a ClassVar. They auto-register into ATOM_REGISTRY via init_subclass.</p> Source code in <code>pyhopper\\Core\\Atoms.py</code> <pre><code>class Atom:\n    \"\"\"Base class for all geometric primitives.\n\n    Subclasses must be frozen dataclasses and define atom_type as a ClassVar.\n    They auto-register into ATOM_REGISTRY via __init_subclass__.\n    \"\"\"\n\n    atom_type: ClassVar[str] = \"Atom\"\n\n    def __init_subclass__(cls, **kwargs):\n        super().__init_subclass__(**kwargs)\n        if hasattr(cls, \"atom_type\") and cls.atom_type != \"Atom\":\n            ATOM_REGISTRY[cls.atom_type] = cls\n\n    def to_json(self) -&gt; dict:\n        raise NotImplementedError\n\n    @classmethod\n    def from_json(cls, data: dict) -&gt; Atom:\n        raise NotImplementedError\n</code></pre>"},{"location":"reference/Core/Atoms/#pyhopper.Core.Atoms.AtomicInterval","title":"<code>AtomicInterval</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Atom</code></p> <p>A numeric domain / interval (Grasshopper's Domain).</p> Source code in <code>pyhopper\\Core\\Atoms.py</code> <pre><code>@dataclass(frozen=True)\nclass AtomicInterval(Atom):\n    \"\"\"A numeric domain / interval (Grasshopper's Domain).\"\"\"\n\n    atom_type: ClassVar[str] = \"Interval\"\n\n    start: float = 0.0\n    end: float = 1.0\n\n    def to_json(self) -&gt; dict:\n        return {\"type\": \"Interval\", \"start\": self.start, \"end\": self.end}\n\n    @classmethod\n    def from_json(cls, data: dict) -&gt; AtomicInterval:\n        return cls(start=data[\"start\"], end=data[\"end\"])\n\n    @property\n    def length(self) -&gt; float:\n        return self.end - self.start\n\n    @property\n    def mid(self) -&gt; float:\n        return (self.start + self.end) / 2.0\n\n    def remap(self, t: float) -&gt; float:\n        \"\"\"Map a value from [0, 1] into this interval.\"\"\"\n        return self.start + t * self.length\n</code></pre>"},{"location":"reference/Core/Atoms/#pyhopper.Core.Atoms.AtomicInterval.remap","title":"<code>remap(t)</code>","text":"<p>Map a value from [0, 1] into this interval.</p> Source code in <code>pyhopper\\Core\\Atoms.py</code> <pre><code>def remap(self, t: float) -&gt; float:\n    \"\"\"Map a value from [0, 1] into this interval.\"\"\"\n    return self.start + t * self.length\n</code></pre>"},{"location":"reference/Core/Atoms/#pyhopper.Core.Atoms.AtomicNurbsCurve","title":"<code>AtomicNurbsCurve</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Atom</code></p> <p>General NURBS curve representation.</p> Source code in <code>pyhopper\\Core\\Atoms.py</code> <pre><code>@dataclass(frozen=True)\nclass AtomicNurbsCurve(Atom):\n    \"\"\"General NURBS curve representation.\"\"\"\n\n    atom_type: ClassVar[str] = \"NurbsCurve\"\n\n    control_points: tuple[AtomicPoint, ...] = ()\n    weights: tuple[float, ...] = ()\n    knots: tuple[float, ...] = ()\n    degree: int = 3\n\n    def to_json(self) -&gt; dict:\n        return {\n            \"type\": \"NurbsCurve\",\n            \"control_points\": [p.to_json() for p in self.control_points],\n            \"weights\": list(self.weights),\n            \"knots\": list(self.knots),\n            \"degree\": self.degree,\n        }\n\n    @classmethod\n    def from_json(cls, data: dict) -&gt; AtomicNurbsCurve:\n        return cls(\n            control_points=tuple(\n                AtomicPoint.from_json(p) for p in data[\"control_points\"]\n            ),\n            weights=tuple(data[\"weights\"]),\n            knots=tuple(data[\"knots\"]),\n            degree=data[\"degree\"],\n        )\n</code></pre>"},{"location":"reference/Core/Atoms/#pyhopper.Core.Atoms.AtomicMesh","title":"<code>AtomicMesh</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Atom</code></p> <p>Polygonal mesh.</p> Source code in <code>pyhopper\\Core\\Atoms.py</code> <pre><code>@dataclass(frozen=True)\nclass AtomicMesh(Atom):\n    \"\"\"Polygonal mesh.\"\"\"\n\n    atom_type: ClassVar[str] = \"Mesh\"\n\n    vertices: tuple[AtomicPoint, ...] = ()\n    faces: tuple[tuple[int, ...], ...] = ()\n\n    def to_json(self) -&gt; dict:\n        return {\n            \"type\": \"Mesh\",\n            \"vertices\": [v.to_json() for v in self.vertices],\n            \"faces\": [list(f) for f in self.faces],\n        }\n\n    @classmethod\n    def from_json(cls, data: dict) -&gt; AtomicMesh:\n        return cls(\n            vertices=tuple(AtomicPoint.from_json(v) for v in data[\"vertices\"]),\n            faces=tuple(tuple(f) for f in data[\"faces\"]),\n        )\n</code></pre>"},{"location":"reference/Core/Atoms/#pyhopper.Core.Atoms.AtomicSurface","title":"<code>AtomicSurface</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Atom</code></p> <p>NURBS surface representation.</p> Source code in <code>pyhopper\\Core\\Atoms.py</code> <pre><code>@dataclass(frozen=True)\nclass AtomicSurface(Atom):\n    \"\"\"NURBS surface representation.\"\"\"\n\n    atom_type: ClassVar[str] = \"Surface\"\n\n    control_points: tuple[tuple[AtomicPoint, ...], ...] = ()\n    degree_u: int = 3\n    degree_v: int = 3\n\n    def to_json(self) -&gt; dict:\n        return {\n            \"type\": \"Surface\",\n            \"control_points\": [\n                [p.to_json() for p in row] for row in self.control_points\n            ],\n            \"degree_u\": self.degree_u,\n            \"degree_v\": self.degree_v,\n        }\n\n    @classmethod\n    def from_json(cls, data: dict) -&gt; AtomicSurface:\n        return cls(\n            control_points=tuple(\n                tuple(AtomicPoint.from_json(p) for p in row)\n                for row in data[\"control_points\"]\n            ),\n            degree_u=data[\"degree_u\"],\n            degree_v=data[\"degree_v\"],\n        )\n</code></pre>"},{"location":"reference/Core/Branch/","title":"Branch","text":""},{"location":"reference/Core/Branch/#pyhopper.Core.Branch","title":"<code>pyhopper.Core.Branch</code>","text":"<p>Branch - A list of items at a specific Path within a DataTree.</p> <p>Subclasses list so all normal list operations work, but carries an immutable path attribute for its location in the tree.</p>"},{"location":"reference/Core/Branch/#pyhopper.Core.Branch.Branch","title":"<code>Branch</code>","text":"<p>               Bases: <code>list</code></p> <p>A list of items located at a specific Path in a DataTree.</p> <p>from pyhopper.Core.Path import Path b = Branch(Path(0, 1), [10, 20, 30]) b.path Path(0, 1) len(b) 3</p> Source code in <code>pyhopper\\Core\\Branch.py</code> <pre><code>class Branch(list):\n    \"\"\"A list of items located at a specific Path in a DataTree.\n\n    &gt;&gt;&gt; from pyhopper.Core.Path import Path\n    &gt;&gt;&gt; b = Branch(Path(0, 1), [10, 20, 30])\n    &gt;&gt;&gt; b.path\n    Path(0, 1)\n    &gt;&gt;&gt; len(b)\n    3\n    \"\"\"\n\n    def __init__(self, path: Path, items: Iterable = ()) -&gt; None:\n        super().__init__(items)\n        self._path = path\n\n    @property\n    def path(self) -&gt; Path:\n        return self._path\n\n    # \u2500\u2500 Serialization \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def to_json(self) -&gt; dict:\n        return {\n            \"path\": self._path.to_json(),\n            \"items\": [_item_to_json(item) for item in self],\n        }\n\n    @classmethod\n    def from_json(cls, data: dict) -&gt; Branch:\n        path = Path.from_json(data[\"path\"])\n        items = [_item_from_json(item) for item in data[\"items\"]]\n        return cls(path, items)\n\n    # \u2500\u2500 Display \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def __repr__(self) -&gt; str:\n        return f\"Branch({self._path!r}, {list.__repr__(self)})\"\n</code></pre>"},{"location":"reference/Core/Component/","title":"Component","text":""},{"location":"reference/Core/Component/#pyhopper.Core.Component","title":"<code>pyhopper.Core.Component</code>","text":"<p>Component - Base class for all pyhopper components.</p> <p>Handles the entire solve pipeline: input coercion, data matching, iteration over branches/items, calling generate(), and output tree construction. Concrete components only implement generate().</p>"},{"location":"reference/Core/Component/#pyhopper.Core.Component.ComponentResult","title":"<code>ComponentResult</code>","text":"<p>               Bases: <code>DataTree</code></p> <p>A DataTree (first output) that also carries sibling outputs.</p> <p>Returned by Component.new. Behaves exactly like a DataTree for the primary output, and exposes secondary outputs as attributes.</p> Source code in <code>pyhopper\\Core\\Component.py</code> <pre><code>class ComponentResult(DataTree):\n    \"\"\"A DataTree (first output) that also carries sibling outputs.\n\n    Returned by Component.__new__. Behaves exactly like a DataTree for\n    the primary output, and exposes secondary outputs as attributes.\n    \"\"\"\n\n    def __init__(\n        self,\n        primary: DataTree,\n        all_outputs: dict[str, DataTree] | None = None,\n    ) -&gt; None:\n        super().__init__(dict(primary._branches))\n        self._all_outputs = all_outputs or {}\n\n    def __getattr__(self, name: str) -&gt; DataTree:\n        if name.startswith(\"_\"):\n            raise AttributeError(name)\n        outputs = object.__getattribute__(self, \"_all_outputs\")\n        if name in outputs:\n            return outputs[name]\n        raise AttributeError(\n            f\"No output named '{name}'. Available: {list(outputs.keys())}\"\n        )\n\n    @property\n    def output_names(self) -&gt; list[str]:\n        return list(self._all_outputs.keys())\n\n    def output(self, name: str) -&gt; DataTree:\n        \"\"\"Access an output by name.\"\"\"\n        return self._all_outputs[name]\n</code></pre>"},{"location":"reference/Core/Component/#pyhopper.Core.Component.ComponentResult.output","title":"<code>output(name)</code>","text":"<p>Access an output by name.</p> Source code in <code>pyhopper\\Core\\Component.py</code> <pre><code>def output(self, name: str) -&gt; DataTree:\n    \"\"\"Access an output by name.\"\"\"\n    return self._all_outputs[name]\n</code></pre>"},{"location":"reference/Core/Component/#pyhopper.Core.Component.Component","title":"<code>Component</code>","text":"<p>Base class for all pyhopper components.</p> Subclasses define <p>inputs  = [InputParam(...), ...] outputs = [OutputParam(...), ...]</p> <p>def generate(self, **matched_inputs) -&gt; value | tuple</p> <p>Calling MyComponent(arg1, arg2, kwarg=val) triggers the solve pipeline and returns a ComponentResult (a DataTree).</p> Source code in <code>pyhopper\\Core\\Component.py</code> <pre><code>class Component:\n    \"\"\"Base class for all pyhopper components.\n\n    Subclasses define:\n        inputs  = [InputParam(...), ...]\n        outputs = [OutputParam(...), ...]\n\n        def generate(self, **matched_inputs) -&gt; value | tuple\n\n    Calling MyComponent(arg1, arg2, kwarg=val) triggers the solve\n    pipeline and returns a ComponentResult (a DataTree).\n    \"\"\"\n\n    inputs: list[InputParam] = []\n    outputs: list[OutputParam] = [OutputParam(\"result\")]\n    match_rule: MatchRule = MatchRule.LONGEST_LIST\n\n    def __new__(cls, *args: Any, **kwargs: Any) -&gt; ComponentResult:\n        instance = object.__new__(cls)\n        return instance._solve(*args, **kwargs)\n\n    def generate(self, **kw: Any) -&gt; Any:\n        \"\"\"Override in subclasses. Receives matched items as keyword args.\n\n        Return a single value (single-output) or a tuple matching\n        len(self.outputs) for multi-output components.\n        \"\"\"\n        raise NotImplementedError(\n            f\"{type(self).__name__} must implement generate()\"\n        )\n\n    # \u2500\u2500 Solve pipeline \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def _solve(self, *args: Any, **kwargs: Any) -&gt; ComponentResult:\n        # 1. Map args/kwargs to declared inputs\n        input_trees = self._coerce_inputs(*args, **kwargs)\n\n        # 2. Determine access mode (use the first input's access, or ITEM)\n        access = self._get_access_mode()\n\n        # 3. Dispatch based on access mode\n        if access == Access.TREE:\n            return self._solve_tree(input_trees)\n        elif access == Access.LIST:\n            return self._solve_list(input_trees)\n        else:\n            return self._solve_item(input_trees)\n\n    def _coerce_inputs(self, *args: Any, **kwargs: Any) -&gt; dict[str, DataTree]:\n        \"\"\"Map positional/keyword args to input names, coerce to DataTrees.\"\"\"\n        input_trees: dict[str, DataTree] = {}\n\n        # Positional args map to inputs in declaration order\n        for i, arg in enumerate(args):\n            if i &lt; len(self.inputs):\n                name = self.inputs[i].name\n                input_trees[name] = DataTree.coerce(arg)\n\n        # Keyword args override\n        for key, val in kwargs.items():\n            input_trees[key] = DataTree.coerce(val)\n\n        # Fill defaults for missing inputs\n        for inp in self.inputs:\n            if inp.name not in input_trees:\n                if inp.default is not None:\n                    input_trees[inp.name] = DataTree.coerce(inp.default)\n                elif not inp.optional:\n                    raise TypeError(\n                        f\"{type(self).__name__} missing required input: '{inp.name}'\"\n                    )\n\n        return input_trees\n\n    def _get_access_mode(self) -&gt; Access:\n        \"\"\"Determine the access mode from input declarations.\"\"\"\n        if not self.inputs:\n            return Access.ITEM\n        # If any input uses TREE access, use TREE\n        for inp in self.inputs:\n            if inp.access == Access.TREE:\n                return Access.TREE\n        # If any input uses LIST access, use LIST\n        for inp in self.inputs:\n            if inp.access == Access.LIST:\n                return Access.LIST\n        return Access.ITEM\n\n    # \u2500\u2500 ITEM access solve \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def _solve_item(self, input_trees: dict[str, DataTree]) -&gt; ComponentResult:\n        \"\"\"Solve with ITEM access: generate called once per matched item-tuple.\"\"\"\n        if not input_trees:\n            # Zero-input component\n            return self._build_result_from_single_call({})\n\n        input_names = [inp.name for inp in self.inputs if inp.name in input_trees]\n        trees = [input_trees[name] for name in input_names]\n\n        # Initialize output collectors\n        output_branches: list[dict[Path, list]] = [\n            {} for _ in self.outputs\n        ]\n\n        for path, matched_items in DataTree.match(trees, self.match_rule):\n            # matched_items[i] is a list of items from trees[i]\n            num_items = len(matched_items[0]) if matched_items else 0\n\n            for item_idx in range(num_items):\n                kw = {}\n                for j, name in enumerate(input_names):\n                    kw[name] = matched_items[j][item_idx]\n\n                result = self.generate(**kw)\n                self._collect_output(result, path, output_branches)\n\n        return self._build_result(output_branches)\n\n    # \u2500\u2500 LIST access solve \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def _solve_list(self, input_trees: dict[str, DataTree]) -&gt; ComponentResult:\n        \"\"\"Solve with LIST access: generate called once per matched branch.\"\"\"\n        if not input_trees:\n            return self._build_result_from_single_call({})\n\n        input_names = [inp.name for inp in self.inputs if inp.name in input_trees]\n        trees = [input_trees[name] for name in input_names]\n\n        output_branches: list[dict[Path, list]] = [\n            {} for _ in self.outputs\n        ]\n\n        for path, matched_items in DataTree.match(trees, self.match_rule):\n            kw = {}\n            for j, name in enumerate(input_names):\n                kw[name] = matched_items[j]  # pass the whole list\n\n            result = self.generate(**kw)\n            self._collect_output(result, path, output_branches)\n\n        return self._build_result(output_branches)\n\n    # \u2500\u2500 TREE access solve \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def _solve_tree(self, input_trees: dict[str, DataTree]) -&gt; ComponentResult:\n        \"\"\"Solve with TREE access: generate called once with full trees.\"\"\"\n        kw = {name: tree for name, tree in input_trees.items()}\n        result = self.generate(**kw)\n\n        output_branches: list[dict[Path, list]] = [\n            {} for _ in self.outputs\n        ]\n        self._collect_output(result, Path.root(), output_branches)\n        return self._build_result(output_branches)\n\n    # \u2500\u2500 Output collection helpers \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def _collect_output(\n        self,\n        result: Any,\n        path: Path,\n        output_branches: list[dict[Path, list]],\n    ) -&gt; None:\n        \"\"\"Place generate() return value(s) into the output branch collectors.\"\"\"\n        num_outputs = len(self.outputs)\n\n        if num_outputs &gt; 1:\n            # Multi-output: result must be a tuple/list matching output count\n            if not isinstance(result, (tuple, list)) or len(result) != num_outputs:\n                raise ValueError(\n                    f\"{type(self).__name__}.generate() must return a tuple of \"\n                    f\"{num_outputs} elements (matching {num_outputs} outputs), \"\n                    f\"got {type(result).__name__}\"\n                )\n            for i, val in enumerate(result):\n                self._add_to_branch(output_branches[i], path, val)\n        else:\n            # Single output\n            self._add_to_branch(output_branches[0], path, result)\n\n    @staticmethod\n    def _add_to_branch(\n        collector: dict[Path, list], path: Path, value: Any\n    ) -&gt; None:\n        \"\"\"Add a value (single item or list of items) to the branch collector.\"\"\"\n        if path not in collector:\n            collector[path] = []\n\n        if isinstance(value, list):\n            collector[path].extend(value)\n        elif isinstance(value, DataTree):\n            # If generate returns a DataTree, merge it\n            collector[path].extend(value.all_items())\n        else:\n            collector[path].append(value)\n\n    def _build_result(\n        self, output_branches: list[dict[Path, list]]\n    ) -&gt; ComponentResult:\n        \"\"\"Build ComponentResult from output branch collectors.\"\"\"\n        output_trees: dict[str, DataTree] = {}\n\n        for i, out_param in enumerate(self.outputs):\n            branches = {}\n            for path, items in output_branches[i].items():\n                branches[path] = Branch(path, items)\n            output_trees[out_param.name] = DataTree(branches)\n\n        first_name = self.outputs[0].name\n        primary = output_trees[first_name]\n        return ComponentResult(primary, output_trees)\n\n    def _build_result_from_single_call(\n        self, kw: dict[str, Any]\n    ) -&gt; ComponentResult:\n        \"\"\"Handle zero-input components.\"\"\"\n        result = self.generate(**kw)\n        output_branches: list[dict[Path, list]] = [\n            {} for _ in self.outputs\n        ]\n        self._collect_output(result, Path.root(), output_branches)\n        return self._build_result(output_branches)\n</code></pre>"},{"location":"reference/Core/Component/#pyhopper.Core.Component.Component.generate","title":"<code>generate(**kw)</code>","text":"<p>Override in subclasses. Receives matched items as keyword args.</p> <p>Return a single value (single-output) or a tuple matching len(self.outputs) for multi-output components.</p> Source code in <code>pyhopper\\Core\\Component.py</code> <pre><code>def generate(self, **kw: Any) -&gt; Any:\n    \"\"\"Override in subclasses. Receives matched items as keyword args.\n\n    Return a single value (single-output) or a tuple matching\n    len(self.outputs) for multi-output components.\n    \"\"\"\n    raise NotImplementedError(\n        f\"{type(self).__name__} must implement generate()\"\n    )\n</code></pre>"},{"location":"reference/Core/DataTree/","title":"DataTree","text":""},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree","title":"<code>pyhopper.Core.DataTree</code>","text":"<p>DataTree - Hierarchical data structure mirroring Grasshopper's DataTree. <p>A DataTree is an ordered collection of Branches, each addressed by a Path. It supports tree operations (graft, flatten, simplify, flip), data matching between multiple trees (longest list, shortest list, cross reference), and JSON serialization.</p>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree","title":"<code>DataTree</code>","text":"<p>Hierarchical data container mirroring Grasshopper's DataTree.</p> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>class DataTree:\n    \"\"\"Hierarchical data container mirroring Grasshopper's DataTree.\"\"\"\n\n    __slots__ = (\"_branches\",)\n\n    def __init__(self, branches: dict[Path, Branch] | None = None) -&gt; None:\n        self._branches: OrderedDict[Path, Branch] = OrderedDict()\n        if branches:\n            for path in sorted(branches.keys()):\n                b = branches[path]\n                if isinstance(b, Branch):\n                    self._branches[path] = b\n                else:\n                    self._branches[path] = Branch(path, b)\n\n    # \u2500\u2500 Constructors \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    @classmethod\n    def from_item(cls, value: Any) -&gt; DataTree:\n        \"\"\"Create a tree with a single item at {0}.\"\"\"\n        path = Path.root()\n        return cls({path: Branch(path, [value])})\n\n    @classmethod\n    def from_list(cls, items: list | tuple) -&gt; DataTree:\n        \"\"\"Create a tree with one branch {0} containing all items.\"\"\"\n        path = Path.root()\n        return cls({path: Branch(path, items)})\n\n    @classmethod\n    def from_branches(cls, data: dict[Path, list]) -&gt; DataTree:\n        \"\"\"Create a tree from an explicit {Path: list} mapping.\"\"\"\n        branches = {}\n        for path, items in data.items():\n            branches[path] = Branch(path, items)\n        return cls(branches)\n\n    @classmethod\n    def coerce(cls, value: Any) -&gt; DataTree:\n        \"\"\"Normalize any value into a DataTree.\n\n        - DataTree -&gt; passthrough\n        - list/tuple -&gt; single branch {0} with all items\n        - scalar/Atom -&gt; single item at {0}\n        \"\"\"\n        if isinstance(value, DataTree):\n            return value\n        if isinstance(value, (list, tuple)):\n            return cls.from_list(value)\n        return cls.from_item(value)\n\n    # \u2500\u2500 Accessors \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    @property\n    def branch_count(self) -&gt; int:\n        return len(self._branches)\n\n    @property\n    def paths(self) -&gt; list[Path]:\n        return list(self._branches.keys())\n\n    def branch(self, path: Path) -&gt; Branch:\n        return self._branches[path]\n\n    def branches(self) -&gt; Iterator[tuple[Path, Branch]]:\n        \"\"\"Yield (path, branch) pairs.\"\"\"\n        yield from self._branches.items()\n\n    def items(self) -&gt; Iterator[tuple[Path, int, Any]]:\n        \"\"\"Yield (path, index, item) for every item in every branch.\"\"\"\n        for path, branch in self._branches.items():\n            for i, item in enumerate(branch):\n                yield path, i, item\n\n    def all_items(self) -&gt; list[Any]:\n        \"\"\"Flat list of all items, ignoring path structure.\"\"\"\n        result = []\n        for branch in self._branches.values():\n            result.extend(branch)\n        return result\n\n    def __iter__(self) -&gt; Iterator[Any]:\n        \"\"\"Iterate over all items (flat), making DataTree iterable.\"\"\"\n        for branch in self._branches.values():\n            yield from branch\n\n    def __len__(self) -&gt; int:\n        \"\"\"Total item count across all branches.\"\"\"\n        return sum(len(b) for b in self._branches.values())\n\n    def __bool__(self) -&gt; bool:\n        return len(self._branches) &gt; 0\n\n    def __contains__(self, item: Any) -&gt; bool:\n        return any(item in b for b in self._branches.values())\n\n    # \u2500\u2500 Tree Operations (all return new DataTree) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def flatten(self) -&gt; DataTree:\n        \"\"\"Collapse all branches into a single branch at {0}.\"\"\"\n        all_items = self.all_items()\n        path = Path.root()\n        return DataTree({path: Branch(path, all_items)})\n\n    def graft(self) -&gt; DataTree:\n        \"\"\"Each item becomes its own branch.\n\n        Item at {a;b}[i] moves to {a;b;i}[0].\n        \"\"\"\n        branches = {}\n        for path, branch in self._branches.items():\n            for i, item in enumerate(branch):\n                new_path = path.append(i)\n                branches[new_path] = Branch(new_path, [item])\n        return DataTree(branches)\n\n    def simplify(self) -&gt; DataTree:\n        \"\"\"Remove the longest common path prefix from all branches.\"\"\"\n        if len(self._branches) &lt;= 1:\n            path = Path.root()\n            items = self.all_items()\n            return DataTree({path: Branch(path, items)})\n\n        all_paths = list(self._branches.keys())\n        prefix = all_paths[0]\n        for p in all_paths[1:]:\n            prefix = prefix.common_prefix(p)\n\n        trim_depth = prefix.depth\n        if trim_depth == 0:\n            return DataTree(dict(self._branches))\n\n        branches = {}\n        for path, branch in self._branches.items():\n            new_path = Path(*path[trim_depth:]) if len(path) &gt; trim_depth else Path.root()\n            branches[new_path] = Branch(new_path, list(branch))\n        return DataTree(branches)\n\n    def flip_matrix(self) -&gt; DataTree:\n        \"\"\"Transpose: swap branch-index and item-index dimensions.\n\n        A tree with N branches of M items each becomes M branches of N items.\n        \"\"\"\n        all_paths = list(self._branches.keys())\n        if not all_paths:\n            return DataTree()\n\n        max_items = max(len(b) for b in self._branches.values())\n        branches = {}\n        for i in range(max_items):\n            new_path = Path(i)\n            items = []\n            for path in all_paths:\n                branch = self._branches[path]\n                if i &lt; len(branch):\n                    items.append(branch[i])\n                elif branch:\n                    items.append(branch[-1])  # repeat last\n            branches[new_path] = Branch(new_path, items)\n        return DataTree(branches)\n\n    def trim(self, depth: int) -&gt; DataTree:\n        \"\"\"Truncate all paths to *depth*, merging branches that collide.\"\"\"\n        branches: dict[Path, list] = {}\n        for path, branch in self._branches.items():\n            new_path = path.trim(depth) if path.depth &gt; depth else path\n            if new_path in branches:\n                branches[new_path].extend(branch)\n            else:\n                branches[new_path] = list(branch)\n        return DataTree.from_branches(branches)\n\n    @classmethod\n    def entwine(cls, *trees: DataTree) -&gt; DataTree:\n        \"\"\"Merge multiple trees, prepending each tree's branches with a unique index.\"\"\"\n        branches = {}\n        for i, tree in enumerate(trees):\n            for path, branch in tree._branches.items():\n                new_path = path.prepend(i)\n                branches[new_path] = Branch(new_path, list(branch))\n        return cls(branches)\n\n    @classmethod\n    def merge(cls, *trees: DataTree) -&gt; DataTree:\n        \"\"\"Combine branches from multiple trees, merging items on path collision.\"\"\"\n        branches: dict[Path, list] = {}\n        for tree in trees:\n            for path, branch in tree._branches.items():\n                if path in branches:\n                    branches[path].extend(branch)\n                else:\n                    branches[path] = list(branch)\n        return cls.from_branches(branches)\n\n    # \u2500\u2500 Data Matching \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    @classmethod\n    def match(\n        cls,\n        trees: list[DataTree],\n        rule: MatchRule = MatchRule.LONGEST_LIST,\n    ) -&gt; Iterator[tuple[Path, list[list[Any]]]]:\n        \"\"\"Match branches and items across multiple input trees.\n\n        Yields (output_path, matched_items_per_input) tuples where\n        matched_items_per_input[i] is the list of items from trees[i]\n        for this iteration step.\n\n        Branch matching uses longest-list (repeat last branch).\n        Item matching within branches uses the specified rule.\n        \"\"\"\n        if not trees:\n            return\n\n        # Determine the principal tree: the one with the most branches,\n        # breaking ties by deepest max path depth. Its paths drive iteration.\n        principal = max(\n            trees,\n            key=lambda t: (t.branch_count, max((p.depth for p in t.paths), default=0)),\n        )\n        sorted_paths = sorted(principal.paths)\n\n        # For each path in the principal tree, find matching branches in others\n        for path in sorted_paths:\n            branches_per_input = []\n            for tree in trees:\n                branch = _find_nearest_branch(tree, path)\n                branches_per_input.append(branch)\n\n            # Match items within matched branches\n            yield from _match_items(path, branches_per_input, rule)\n\n    # \u2500\u2500 Serialization \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def to_json(self) -&gt; dict:\n        return {\n            \"type\": \"DataTree\",\n            \"branches\": [b.to_json() for b in self._branches.values()],\n        }\n\n    @classmethod\n    def from_json(cls, data: dict) -&gt; DataTree:\n        branches = {}\n        for b_data in data[\"branches\"]:\n            branch = Branch.from_json(b_data)\n            branches[branch.path] = branch\n        return cls(branches)\n\n    def to_json_string(self, indent: int = 2) -&gt; str:\n        return json.dumps(self.to_json(), indent=indent)\n\n    # \u2500\u2500 Display \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def __repr__(self) -&gt; str:\n        lines = [f\"DataTree ({self.branch_count} branches, {len(self)} items):\"]\n        for path, branch in self._branches.items():\n            items_repr = \", \".join(repr(item) for item in branch[:5])\n            if len(branch) &gt; 5:\n                items_repr += f\", ... ({len(branch)} total)\"\n            lines.append(f\"  {path}: [{items_repr}]\")\n        return \"\\n\".join(lines)\n</code></pre>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree.from_item","title":"<code>from_item(value)</code>  <code>classmethod</code>","text":"<p>Create a tree with a single item at {0}.</p> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>@classmethod\ndef from_item(cls, value: Any) -&gt; DataTree:\n    \"\"\"Create a tree with a single item at {0}.\"\"\"\n    path = Path.root()\n    return cls({path: Branch(path, [value])})\n</code></pre>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree.from_list","title":"<code>from_list(items)</code>  <code>classmethod</code>","text":"<p>Create a tree with one branch {0} containing all items.</p> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>@classmethod\ndef from_list(cls, items: list | tuple) -&gt; DataTree:\n    \"\"\"Create a tree with one branch {0} containing all items.\"\"\"\n    path = Path.root()\n    return cls({path: Branch(path, items)})\n</code></pre>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree.from_branches","title":"<code>from_branches(data)</code>  <code>classmethod</code>","text":"<p>Create a tree from an explicit {Path: list} mapping.</p> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>@classmethod\ndef from_branches(cls, data: dict[Path, list]) -&gt; DataTree:\n    \"\"\"Create a tree from an explicit {Path: list} mapping.\"\"\"\n    branches = {}\n    for path, items in data.items():\n        branches[path] = Branch(path, items)\n    return cls(branches)\n</code></pre>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree.coerce","title":"<code>coerce(value)</code>  <code>classmethod</code>","text":"<p>Normalize any value into a DataTree.</p> <ul> <li>DataTree -&gt; passthrough</li> <li>list/tuple -&gt; single branch {0} with all items</li> <li>scalar/Atom -&gt; single item at {0}</li> </ul> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>@classmethod\ndef coerce(cls, value: Any) -&gt; DataTree:\n    \"\"\"Normalize any value into a DataTree.\n\n    - DataTree -&gt; passthrough\n    - list/tuple -&gt; single branch {0} with all items\n    - scalar/Atom -&gt; single item at {0}\n    \"\"\"\n    if isinstance(value, DataTree):\n        return value\n    if isinstance(value, (list, tuple)):\n        return cls.from_list(value)\n    return cls.from_item(value)\n</code></pre>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree.branches","title":"<code>branches()</code>","text":"<p>Yield (path, branch) pairs.</p> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>def branches(self) -&gt; Iterator[tuple[Path, Branch]]:\n    \"\"\"Yield (path, branch) pairs.\"\"\"\n    yield from self._branches.items()\n</code></pre>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree.items","title":"<code>items()</code>","text":"<p>Yield (path, index, item) for every item in every branch.</p> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>def items(self) -&gt; Iterator[tuple[Path, int, Any]]:\n    \"\"\"Yield (path, index, item) for every item in every branch.\"\"\"\n    for path, branch in self._branches.items():\n        for i, item in enumerate(branch):\n            yield path, i, item\n</code></pre>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree.all_items","title":"<code>all_items()</code>","text":"<p>Flat list of all items, ignoring path structure.</p> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>def all_items(self) -&gt; list[Any]:\n    \"\"\"Flat list of all items, ignoring path structure.\"\"\"\n    result = []\n    for branch in self._branches.values():\n        result.extend(branch)\n    return result\n</code></pre>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree.__iter__","title":"<code>__iter__()</code>","text":"<p>Iterate over all items (flat), making DataTree iterable.</p> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>def __iter__(self) -&gt; Iterator[Any]:\n    \"\"\"Iterate over all items (flat), making DataTree iterable.\"\"\"\n    for branch in self._branches.values():\n        yield from branch\n</code></pre>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree.__len__","title":"<code>__len__()</code>","text":"<p>Total item count across all branches.</p> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>def __len__(self) -&gt; int:\n    \"\"\"Total item count across all branches.\"\"\"\n    return sum(len(b) for b in self._branches.values())\n</code></pre>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree.flatten","title":"<code>flatten()</code>","text":"<p>Collapse all branches into a single branch at {0}.</p> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>def flatten(self) -&gt; DataTree:\n    \"\"\"Collapse all branches into a single branch at {0}.\"\"\"\n    all_items = self.all_items()\n    path = Path.root()\n    return DataTree({path: Branch(path, all_items)})\n</code></pre>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree.graft","title":"<code>graft()</code>","text":"<p>Each item becomes its own branch.</p> <p>Item at {a;b}[i] moves to {a;b;i}[0].</p> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>def graft(self) -&gt; DataTree:\n    \"\"\"Each item becomes its own branch.\n\n    Item at {a;b}[i] moves to {a;b;i}[0].\n    \"\"\"\n    branches = {}\n    for path, branch in self._branches.items():\n        for i, item in enumerate(branch):\n            new_path = path.append(i)\n            branches[new_path] = Branch(new_path, [item])\n    return DataTree(branches)\n</code></pre>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree.simplify","title":"<code>simplify()</code>","text":"<p>Remove the longest common path prefix from all branches.</p> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>def simplify(self) -&gt; DataTree:\n    \"\"\"Remove the longest common path prefix from all branches.\"\"\"\n    if len(self._branches) &lt;= 1:\n        path = Path.root()\n        items = self.all_items()\n        return DataTree({path: Branch(path, items)})\n\n    all_paths = list(self._branches.keys())\n    prefix = all_paths[0]\n    for p in all_paths[1:]:\n        prefix = prefix.common_prefix(p)\n\n    trim_depth = prefix.depth\n    if trim_depth == 0:\n        return DataTree(dict(self._branches))\n\n    branches = {}\n    for path, branch in self._branches.items():\n        new_path = Path(*path[trim_depth:]) if len(path) &gt; trim_depth else Path.root()\n        branches[new_path] = Branch(new_path, list(branch))\n    return DataTree(branches)\n</code></pre>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree.flip_matrix","title":"<code>flip_matrix()</code>","text":"<p>Transpose: swap branch-index and item-index dimensions.</p> <p>A tree with N branches of M items each becomes M branches of N items.</p> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>def flip_matrix(self) -&gt; DataTree:\n    \"\"\"Transpose: swap branch-index and item-index dimensions.\n\n    A tree with N branches of M items each becomes M branches of N items.\n    \"\"\"\n    all_paths = list(self._branches.keys())\n    if not all_paths:\n        return DataTree()\n\n    max_items = max(len(b) for b in self._branches.values())\n    branches = {}\n    for i in range(max_items):\n        new_path = Path(i)\n        items = []\n        for path in all_paths:\n            branch = self._branches[path]\n            if i &lt; len(branch):\n                items.append(branch[i])\n            elif branch:\n                items.append(branch[-1])  # repeat last\n        branches[new_path] = Branch(new_path, items)\n    return DataTree(branches)\n</code></pre>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree.trim","title":"<code>trim(depth)</code>","text":"<p>Truncate all paths to depth, merging branches that collide.</p> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>def trim(self, depth: int) -&gt; DataTree:\n    \"\"\"Truncate all paths to *depth*, merging branches that collide.\"\"\"\n    branches: dict[Path, list] = {}\n    for path, branch in self._branches.items():\n        new_path = path.trim(depth) if path.depth &gt; depth else path\n        if new_path in branches:\n            branches[new_path].extend(branch)\n        else:\n            branches[new_path] = list(branch)\n    return DataTree.from_branches(branches)\n</code></pre>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree.entwine","title":"<code>entwine(*trees)</code>  <code>classmethod</code>","text":"<p>Merge multiple trees, prepending each tree's branches with a unique index.</p> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>@classmethod\ndef entwine(cls, *trees: DataTree) -&gt; DataTree:\n    \"\"\"Merge multiple trees, prepending each tree's branches with a unique index.\"\"\"\n    branches = {}\n    for i, tree in enumerate(trees):\n        for path, branch in tree._branches.items():\n            new_path = path.prepend(i)\n            branches[new_path] = Branch(new_path, list(branch))\n    return cls(branches)\n</code></pre>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree.merge","title":"<code>merge(*trees)</code>  <code>classmethod</code>","text":"<p>Combine branches from multiple trees, merging items on path collision.</p> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>@classmethod\ndef merge(cls, *trees: DataTree) -&gt; DataTree:\n    \"\"\"Combine branches from multiple trees, merging items on path collision.\"\"\"\n    branches: dict[Path, list] = {}\n    for tree in trees:\n        for path, branch in tree._branches.items():\n            if path in branches:\n                branches[path].extend(branch)\n            else:\n                branches[path] = list(branch)\n    return cls.from_branches(branches)\n</code></pre>"},{"location":"reference/Core/DataTree/#pyhopper.Core.DataTree.DataTree.match","title":"<code>match(trees, rule=MatchRule.LONGEST_LIST)</code>  <code>classmethod</code>","text":"<p>Match branches and items across multiple input trees.</p> <p>Yields (output_path, matched_items_per_input) tuples where matched_items_per_input[i] is the list of items from trees[i] for this iteration step.</p> <p>Branch matching uses longest-list (repeat last branch). Item matching within branches uses the specified rule.</p> Source code in <code>pyhopper\\Core\\DataTree.py</code> <pre><code>@classmethod\ndef match(\n    cls,\n    trees: list[DataTree],\n    rule: MatchRule = MatchRule.LONGEST_LIST,\n) -&gt; Iterator[tuple[Path, list[list[Any]]]]:\n    \"\"\"Match branches and items across multiple input trees.\n\n    Yields (output_path, matched_items_per_input) tuples where\n    matched_items_per_input[i] is the list of items from trees[i]\n    for this iteration step.\n\n    Branch matching uses longest-list (repeat last branch).\n    Item matching within branches uses the specified rule.\n    \"\"\"\n    if not trees:\n        return\n\n    # Determine the principal tree: the one with the most branches,\n    # breaking ties by deepest max path depth. Its paths drive iteration.\n    principal = max(\n        trees,\n        key=lambda t: (t.branch_count, max((p.depth for p in t.paths), default=0)),\n    )\n    sorted_paths = sorted(principal.paths)\n\n    # For each path in the principal tree, find matching branches in others\n    for path in sorted_paths:\n        branches_per_input = []\n        for tree in trees:\n            branch = _find_nearest_branch(tree, path)\n            branches_per_input.append(branch)\n\n        # Match items within matched branches\n        yield from _match_items(path, branches_per_input, rule)\n</code></pre>"},{"location":"reference/Core/Path/","title":"Path","text":""},{"location":"reference/Core/Path/#pyhopper.Core.Path","title":"<code>pyhopper.Core.Path</code>","text":"<p>Path - Immutable address for a branch within a DataTree.</p> <p>Mirrors Grasshopper's GH_Path: a tuple of non-negative integers representing a hierarchical location like {0;2;1}.</p>"},{"location":"reference/Core/Path/#pyhopper.Core.Path.Path","title":"<code>Path</code>","text":"<p>               Bases: <code>tuple</code></p> <p>Immutable branch address in a DataTree.</p> <p>p = Path(0, 2, 1) str(p) '{0;2;1}' p.depth 3</p> Source code in <code>pyhopper\\Core\\Path.py</code> <pre><code>class Path(tuple):\n    \"\"\"Immutable branch address in a DataTree.\n\n    &gt;&gt;&gt; p = Path(0, 2, 1)\n    &gt;&gt;&gt; str(p)\n    '{0;2;1}'\n    &gt;&gt;&gt; p.depth\n    3\n    \"\"\"\n\n    def __new__(cls, *indices: int) -&gt; Path:\n        if len(indices) == 1 and isinstance(indices[0], (list, tuple)):\n            indices = tuple(indices[0])\n        return super().__new__(cls, indices)\n\n    # \u2500\u2500 Class constructors \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    @classmethod\n    def root(cls) -&gt; Path:\n        \"\"\"The default single-branch path {0}.\"\"\"\n        return cls(0)\n\n    @classmethod\n    def parse(cls, s: str) -&gt; Path:\n        \"\"\"Parse a Grasshopper-style path string like '{0;2;1}'.\"\"\"\n        s = s.strip().strip(\"{}\")\n        if not s:\n            return cls(0)\n        return cls(*(int(x) for x in s.split(\";\")))\n\n    @classmethod\n    def from_json(cls, data: list[int]) -&gt; Path:\n        \"\"\"Reconstruct from a JSON-serializable list.\"\"\"\n        return cls(*data)\n\n    # \u2500\u2500 Properties \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    @property\n    def depth(self) -&gt; int:\n        \"\"\"Number of indices in this path.\"\"\"\n        return len(self)\n\n    # \u2500\u2500 Operations (all return new Path, never mutate) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def append(self, index: int) -&gt; Path:\n        \"\"\"Return a new Path with *index* appended at the end.\"\"\"\n        return Path(*self, index)\n\n    def prepend(self, index: int) -&gt; Path:\n        \"\"\"Return a new Path with *index* prepended at the start.\"\"\"\n        return Path(index, *self)\n\n    def trim(self, depth: int) -&gt; Path:\n        \"\"\"Return a new Path truncated to the first *depth* elements.\"\"\"\n        return Path(*self[:depth])\n\n    def common_prefix(self, other: Path) -&gt; Path:\n        \"\"\"Return the longest shared prefix between this path and *other*.\"\"\"\n        shared = []\n        for a, b in zip(self, other):\n            if a != b:\n                break\n            shared.append(a)\n        return Path(*shared) if shared else Path(0)\n\n    # \u2500\u2500 Serialization \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def to_json(self) -&gt; list[int]:\n        \"\"\"Serialize to a JSON-compatible list of ints.\"\"\"\n        return list(self)\n\n    # \u2500\u2500 Display \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def __str__(self) -&gt; str:\n        return \"{\" + \";\".join(str(i) for i in self) + \"}\"\n\n    def __repr__(self) -&gt; str:\n        return f\"Path({', '.join(str(i) for i in self)})\"\n</code></pre>"},{"location":"reference/Core/Path/#pyhopper.Core.Path.Path.depth","title":"<code>depth</code>  <code>property</code>","text":"<p>Number of indices in this path.</p>"},{"location":"reference/Core/Path/#pyhopper.Core.Path.Path.root","title":"<code>root()</code>  <code>classmethod</code>","text":"<p>The default single-branch path {0}.</p> Source code in <code>pyhopper\\Core\\Path.py</code> <pre><code>@classmethod\ndef root(cls) -&gt; Path:\n    \"\"\"The default single-branch path {0}.\"\"\"\n    return cls(0)\n</code></pre>"},{"location":"reference/Core/Path/#pyhopper.Core.Path.Path.parse","title":"<code>parse(s)</code>  <code>classmethod</code>","text":"<p>Parse a Grasshopper-style path string like '{0;2;1}'.</p> Source code in <code>pyhopper\\Core\\Path.py</code> <pre><code>@classmethod\ndef parse(cls, s: str) -&gt; Path:\n    \"\"\"Parse a Grasshopper-style path string like '{0;2;1}'.\"\"\"\n    s = s.strip().strip(\"{}\")\n    if not s:\n        return cls(0)\n    return cls(*(int(x) for x in s.split(\";\")))\n</code></pre>"},{"location":"reference/Core/Path/#pyhopper.Core.Path.Path.from_json","title":"<code>from_json(data)</code>  <code>classmethod</code>","text":"<p>Reconstruct from a JSON-serializable list.</p> Source code in <code>pyhopper\\Core\\Path.py</code> <pre><code>@classmethod\ndef from_json(cls, data: list[int]) -&gt; Path:\n    \"\"\"Reconstruct from a JSON-serializable list.\"\"\"\n    return cls(*data)\n</code></pre>"},{"location":"reference/Core/Path/#pyhopper.Core.Path.Path.append","title":"<code>append(index)</code>","text":"<p>Return a new Path with index appended at the end.</p> Source code in <code>pyhopper\\Core\\Path.py</code> <pre><code>def append(self, index: int) -&gt; Path:\n    \"\"\"Return a new Path with *index* appended at the end.\"\"\"\n    return Path(*self, index)\n</code></pre>"},{"location":"reference/Core/Path/#pyhopper.Core.Path.Path.prepend","title":"<code>prepend(index)</code>","text":"<p>Return a new Path with index prepended at the start.</p> Source code in <code>pyhopper\\Core\\Path.py</code> <pre><code>def prepend(self, index: int) -&gt; Path:\n    \"\"\"Return a new Path with *index* prepended at the start.\"\"\"\n    return Path(index, *self)\n</code></pre>"},{"location":"reference/Core/Path/#pyhopper.Core.Path.Path.trim","title":"<code>trim(depth)</code>","text":"<p>Return a new Path truncated to the first depth elements.</p> Source code in <code>pyhopper\\Core\\Path.py</code> <pre><code>def trim(self, depth: int) -&gt; Path:\n    \"\"\"Return a new Path truncated to the first *depth* elements.\"\"\"\n    return Path(*self[:depth])\n</code></pre>"},{"location":"reference/Core/Path/#pyhopper.Core.Path.Path.common_prefix","title":"<code>common_prefix(other)</code>","text":"<p>Return the longest shared prefix between this path and other.</p> Source code in <code>pyhopper\\Core\\Path.py</code> <pre><code>def common_prefix(self, other: Path) -&gt; Path:\n    \"\"\"Return the longest shared prefix between this path and *other*.\"\"\"\n    shared = []\n    for a, b in zip(self, other):\n        if a != b:\n            break\n        shared.append(a)\n    return Path(*shared) if shared else Path(0)\n</code></pre>"},{"location":"reference/Core/Path/#pyhopper.Core.Path.Path.to_json","title":"<code>to_json()</code>","text":"<p>Serialize to a JSON-compatible list of ints.</p> Source code in <code>pyhopper\\Core\\Path.py</code> <pre><code>def to_json(self) -&gt; list[int]:\n    \"\"\"Serialize to a JSON-compatible list of ints.\"\"\"\n    return list(self)\n</code></pre>"},{"location":"reference/Utils/Exporters/glb/","title":"glb","text":""},{"location":"reference/Utils/Exporters/glb/#pyhopper.Utils.Exporters.glb","title":"<code>pyhopper.Utils.Exporters.glb</code>","text":"<p>GLB exporter for pyhopper DataTrees.</p> <p>Converts a DataTree of Atom primitives into a binary glTF 2.0 (.glb) file.</p>"},{"location":"reference/Utils/Exporters/glb/#pyhopper.Utils.Exporters.glb--supported-atoms","title":"Supported atoms","text":"<p>AtomicPolyline  -&gt; LINE_STRIP primitive (geometry only, no material needed)   AtomicLine      -&gt; LINE_STRIP primitive (2 points)   AtomicCylinder  -&gt; tessellated triangle mesh   AtomicMesh      -&gt; triangle mesh (faces must be triangles or quads; quads split)   AtomicPoint     -&gt; POINTS primitive   AtomicCircle    -&gt; tessellated LINE_STRIP (36 segments)</p> <p>Unknown atom types are silently skipped.</p>"},{"location":"reference/Utils/Exporters/glb/#pyhopper.Utils.Exporters.glb.export_glb","title":"<code>export_glb(tree, filepath)</code>","text":"<p>Export tree (DataTree or plain list of Atoms) to a .glb file.</p> Source code in <code>pyhopper\\Utils\\Exporters\\glb.py</code> <pre><code>def export_glb(tree: Any, filepath: str | FilePath) -&gt; None:\n    \"\"\"Export *tree* (DataTree or plain list of Atoms) to a .glb file.\"\"\"\n    from pyhopper.Core.Atoms import (\n        AtomicCircle,\n        AtomicCylinder,\n        AtomicLine,\n        AtomicMesh,\n        AtomicPoint,\n        AtomicPolyline,\n    )\n    from pyhopper.Core.DataTree import DataTree\n\n    if isinstance(tree, DataTree):\n        atoms = tree.all_items()\n    elif isinstance(tree, (list, tuple)):\n        atoms = list(tree)\n    else:\n        atoms = [tree]\n\n    builder = _GlbBuilder()\n\n    for atom in atoms:\n        if isinstance(atom, AtomicPolyline):\n            builder.add_polyline(atom)\n        elif isinstance(atom, AtomicLine):\n            builder.add_line(atom)\n        elif isinstance(atom, AtomicCylinder):\n            builder.add_cylinder(atom)\n        elif isinstance(atom, AtomicMesh):\n            builder.add_mesh(atom)\n        elif isinstance(atom, AtomicPoint):\n            builder.add_point(atom)\n        elif isinstance(atom, AtomicCircle):\n            builder.add_circle(atom)\n\n    data = builder.build()\n    FilePath(filepath).write_bytes(data)\n</code></pre>"}]}