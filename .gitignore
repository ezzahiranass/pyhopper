# Python virtual environment (created by local setup)
.venv/
# Python bytecode
__pycache__/
*.pyc
# vendored third-party download and scratch archives
three-viewport-gizmo-main/
cd_ph2.zip

# mkdocs build output
site/

# packaging / docs output
*.egg-info/
# local scratch (the owner's archive of retired folders and probe leftovers)
archive/
_tmp_*.glb
log.txt
run_docs.txt
# Claude Code personal settings (launch.json stays tracked)
.claude/settings.json
.claude/settings.local.json
