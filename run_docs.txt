# pyhopper — local run notes (Windows, PowerShell or cmd)
# Python deps live in the repo-local virtualenv .venv (Python 3.11).
# One-time setup (already done on this machine):
#   python -m venv .venv
#   .venv\Scripts\python -m pip install -e . -r pyhopper-web\api\requirements.txt shapely
#   .venv\Scripts\python -m pip install mkdocs mkdocs-material "mkdocstrings[python]" mkdocs-gen-files mkdocs-literate-nav pymdown-extensions

# Docs (served on 8001 — port 8000 is taken by the FastAPI backend)
d:
cd D:\Apps\pyhopper
.venv\Scripts\mkdocs build --strict
.venv\Scripts\mkdocs serve -a 127.0.0.1:8001

# Core library tests + tower example
d:
cd D:\Apps\pyhopper
.venv\Scripts\python -m unittest discover -s tests -t . -v
.venv\Scripts\python pyhopper\Examples\first_test.py
